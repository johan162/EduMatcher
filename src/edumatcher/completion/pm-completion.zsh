# GENERATED by tools/gen_completion.py -- do not edit. Regenerate with: make completion
# Load with: eval "$(<this file)"  -- after compinit; `source` does NOT work

#compdef pm-admin

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-admin (e.g. ~/.local/share/zsh_completion/_pm-admin).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_admin_commands() {
  local _commands=(
    
  )
  _describe 'pm-admin commands' _commands
}

_shtab_pm_admin_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--id[ADMIN gateway ID (e.g. GW_ADMIN)]:GW_ID:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_defaults_added=0


_shtab_pm_admin() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-admin'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_admin_defaults_added )); then
    if (( ${_shtab_pm_admin_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_admin_options[(I)${(q)remainder}*]} +          ${_shtab_pm_admin_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_admin_options+=(': :_shtab_pm_admin_commands' '*::: :->pm-admin')
    fi
    _shtab_pm_admin_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_admin_options

  case $state in
    pm-admin)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_admin-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_admin -N pm-admin
else
  # autoload from fpath, call function directly
  _shtab_pm_admin "$@"
fi

#compdef pm-admin-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-admin-cli (e.g. ~/.local/share/zsh_completion/_pm-admin-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_admin_cli_commands() {
  local _commands=(
    book:'Print the L1/L2 order-book snapshot for a symbol'
    cancel-sym:'Cancel all resting orders on a symbol across every gateway (ADMIN role required)'
    gateways:'List all configured gateways and their connection status'
    halt:'Exchange-wide circuit-breaker halt (ADMIN role required)'
    halt-sym:'Halt trading on a single symbol (ADMIN role required)'
    kick:'Forcefully disconnect a gateway'
    kill:'Cancel all resting orders and quotes for a gateway'
    level:'Show every resting order making up a symbol (or one price level), across all gateways (ADMIN role required)'
    orders:'List all resting orders for a gateway'
    position:'Show net position, avg cost, and live bid/ask/spread/mid per symbol for a gateway (any gateway, not just a market maker)'
    qcancel:'Cancel the active two-sided quote for a gateway on one symbol'
    reopen:'Force one symbol to uncross now and clear any halt in one step (ADMIN role required)'
    resume:'Resume all symbols halted by '"'"'halt'"'"' (ADMIN role required)'
    resume-sym:'Resume a single symbol halted by '"'"'halt-sym'"'"' or a circuit breaker (ADMIN role required)'
    schedule:'Show the automatic session-transition schedule from the engine config'
    session:'Request a session-phase transition'
    session-status:'Show the current session state (read-only, no transition)'
    symbols:'List all instruments configured in the engine'
    volume:'Show daily traded volume per symbol and exchange total'
  )
  _describe 'pm-admin-cli commands' _commands
}

_shtab_pm_admin_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--id[ADMIN gateway ID configured in engine_config.yaml (e.g. GW_ADMIN)]:GW_ID:"
  "--push[Engine PULL socket address (default\: tcp\:\/\/127.0.0.1\:5555)]:ADDR:"
  "--sub[Engine PUB socket address (default\: tcp\:\/\/127.0.0.1\:5556)]:ADDR:"
  "--timeout[Ack timeout in milliseconds (default\: 3000)]:MS:"
  "--format[Output format for the command\'s result\: \'text\' (default, human-readable) or \'json\' (machine-readable, printed to stdout)]:format:(text json)"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_defaults_added=0

_shtab_pm_admin_cli_book_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--sym[Symbol to query]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_book_defaults_added=0

_shtab_pm_admin_cli_cancel_sym_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--sym[Symbol to clear]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_cancel_sym_defaults_added=0

_shtab_pm_admin_cli_gateways_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_gateways_defaults_added=0

_shtab_pm_admin_cli_halt_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_halt_defaults_added=0

_shtab_pm_admin_cli_halt_sym_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--sym[Symbol to halt]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_halt_sym_defaults_added=0

_shtab_pm_admin_cli_kick_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gw[Target gateway ID]:GW_ID:"
  "--reason[Reason string recorded in the engine log]:TEXT:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_kick_defaults_added=0

_shtab_pm_admin_cli_kill_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gw[Target gateway ID]:GW_ID:"
  "--sym[Scope cancellation to one symbol (omit for all symbols)]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_kill_defaults_added=0

_shtab_pm_admin_cli_level_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--sym[Symbol to query]:SYMBOL:"
  "--price[Narrow to orders resting at exactly this price (omit for the whole symbol)]:PRICE:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_level_defaults_added=0

_shtab_pm_admin_cli_orders_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gw[Target gateway ID]:GW_ID:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_orders_defaults_added=0

_shtab_pm_admin_cli_position_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gw[Target gateway ID]:GW_ID:"
  "--sym[Narrow to one or more comma-separated symbols (omit for all)]:SYMBOL[,SYMBOL...]:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_position_defaults_added=0

_shtab_pm_admin_cli_qcancel_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gw[Target gateway ID]:GW_ID:"
  "--sym[Symbol to cancel]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_qcancel_defaults_added=0

_shtab_pm_admin_cli_reopen_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--sym[Symbol to reopen]:SYMBOL:"
  "--price[Operator opening price\; omit to open at the naturally computed equilibrium]:PRICE:"
  "--dry-run[Peek the indicative print without changing any state]"
  "--note[Reason string recorded in the engine log]:TEXT:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_reopen_defaults_added=0

_shtab_pm_admin_cli_resume_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_resume_defaults_added=0

_shtab_pm_admin_cli_resume_sym_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--sym[Symbol to resume]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_resume_sym_defaults_added=0

_shtab_pm_admin_cli_schedule_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_schedule_defaults_added=0

_shtab_pm_admin_cli_session_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--state[Target session state. One of\: PRE_OPEN OPENING_AUCTION CONTINUOUS CLOSING_AUCTION CLOSED]:STATE:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_session_defaults_added=0

_shtab_pm_admin_cli_session_status_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_session_status_defaults_added=0

_shtab_pm_admin_cli_symbols_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_symbols_defaults_added=0

_shtab_pm_admin_cli_volume_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_admin_cli_volume_defaults_added=0


_shtab_pm_admin_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-admin-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_admin_cli_defaults_added )); then
    if (( ${_shtab_pm_admin_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_admin_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_admin_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_admin_cli_options+=(': :_shtab_pm_admin_cli_commands' '*::: :->pm-admin-cli')
    fi
    _shtab_pm_admin_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_admin_cli_options

  case $state in
    pm-admin-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_admin_cli-$line[1]:"
      case $line[1] in
        book) _arguments -C -s $_shtab_pm_admin_cli_book_options ;;
        cancel-sym) _arguments -C -s $_shtab_pm_admin_cli_cancel_sym_options ;;
        gateways) _arguments -C -s $_shtab_pm_admin_cli_gateways_options ;;
        halt) _arguments -C -s $_shtab_pm_admin_cli_halt_options ;;
        halt-sym) _arguments -C -s $_shtab_pm_admin_cli_halt_sym_options ;;
        kick) _arguments -C -s $_shtab_pm_admin_cli_kick_options ;;
        kill) _arguments -C -s $_shtab_pm_admin_cli_kill_options ;;
        level) _arguments -C -s $_shtab_pm_admin_cli_level_options ;;
        orders) _arguments -C -s $_shtab_pm_admin_cli_orders_options ;;
        position) _arguments -C -s $_shtab_pm_admin_cli_position_options ;;
        qcancel) _arguments -C -s $_shtab_pm_admin_cli_qcancel_options ;;
        reopen) _arguments -C -s $_shtab_pm_admin_cli_reopen_options ;;
        resume) _arguments -C -s $_shtab_pm_admin_cli_resume_options ;;
        resume-sym) _arguments -C -s $_shtab_pm_admin_cli_resume_sym_options ;;
        schedule) _arguments -C -s $_shtab_pm_admin_cli_schedule_options ;;
        session) _arguments -C -s $_shtab_pm_admin_cli_session_options ;;
        session-status) _arguments -C -s $_shtab_pm_admin_cli_session_status_options ;;
        symbols) _arguments -C -s $_shtab_pm_admin_cli_symbols_options ;;
        volume) _arguments -C -s $_shtab_pm_admin_cli_volume_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_admin_cli -N pm-admin-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_admin_cli "$@"
fi

#compdef pm-ai-swarm

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-ai-swarm (e.g. ~/.local/share/zsh_completion/_pm-ai-swarm).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_ai_swarm_commands() {
  local _commands=(
    
  )
  _describe 'pm-ai-swarm commands' _commands
}

_shtab_pm_ai_swarm_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--count[Number of bots to launch]:count:"
  "--prefix[Gateway id prefix, e.g. AI]:prefix:"
  "--start-index[start_index]:start_index:"
  "--profiles[Comma-separated profile cycle. Default\: all available profiles]:profiles:"
  "--symbols[Comma-separated symbols\; default loads all symbols from engine config]:symbols:"
  "--seed-base[seed_base]:seed_base:"
  "--duration[duration]:duration:"
  "--python[python]:python:"
  "--max-position[max_position]:max_position:"
  "--max-rejects[max_rejects]:max_rejects:"
  "--reject-window[reject_window]:reject_window:"
  "--reject-cooldown[reject_cooldown]:reject_cooldown:"
  "--stale-data[stale_data]:stale_data:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase verbosity (-v\: INFO, -vv\: DEBUG)\; forwarded to child bots]"
  {-q,--quiet}"[Reduce output to warnings\/errors\; forwarded to child bots]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_ai_swarm_defaults_added=0


_shtab_pm_ai_swarm() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-ai-swarm'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_ai_swarm_defaults_added )); then
    if (( ${_shtab_pm_ai_swarm_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_ai_swarm_options[(I)${(q)remainder}*]} +          ${_shtab_pm_ai_swarm_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_ai_swarm_options+=(': :_shtab_pm_ai_swarm_commands' '*::: :->pm-ai-swarm')
    fi
    _shtab_pm_ai_swarm_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_ai_swarm_options

  case $state in
    pm-ai-swarm)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_ai_swarm-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_ai_swarm -N pm-ai-swarm
else
  # autoload from fpath, call function directly
  _shtab_pm_ai_swarm "$@"
fi

#compdef pm-ai-trader

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-ai-trader (e.g. ~/.local/share/zsh_completion/_pm-ai-trader).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_ai_trader_commands() {
  local _commands=(
    
  )
  _describe 'pm-ai-trader commands' _commands
}

_shtab_pm_ai_trader_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--id[Gateway ID, e.g. AI01]:id:"
  "--profile[Personality profile]:profile:(aggressive cautious few-large many-small)"
  "--symbols[Optional comma-separated symbol allowlist, e.g. AAPL,MSFT]:symbols:"
  "--seed[Random seed for deterministic behavior]:seed:"
  "--duration[Run duration in seconds\; 0 means run until stopped]:duration:"
  "--run-id[Optional run identifier, autogenerated if omitted]:run_id:"
  "--max-position[Absolute position limit per symbol]:max_position:"
  "--max-rejects[Reject count threshold for breaker]:max_rejects:"
  "--reject-window[Rolling window in seconds for reject threshold]:reject_window:"
  "--reject-cooldown[Pause duration in seconds after reject breaker trips]:reject_cooldown:"
  "--stale-data[Max age in seconds for market data before pausing submissions]:stale_data:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase verbosity (-v\: INFO \+ bot debug prints, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_ai_trader_defaults_added=0


_shtab_pm_ai_trader() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-ai-trader'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_ai_trader_defaults_added )); then
    if (( ${_shtab_pm_ai_trader_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_ai_trader_options[(I)${(q)remainder}*]} +          ${_shtab_pm_ai_trader_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_ai_trader_options+=(': :_shtab_pm_ai_trader_commands' '*::: :->pm-ai-trader')
    fi
    _shtab_pm_ai_trader_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_ai_trader_options

  case $state in
    pm-ai-trader)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_ai_trader-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_ai_trader -N pm-ai-trader
else
  # autoload from fpath, call function directly
  _shtab_pm_ai_trader "$@"
fi

#compdef pm-alf-console

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-alf-console (e.g. ~/.local/share/zsh_completion/_pm-alf-console).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_alf_console_commands() {
  local _commands=(
    
  )
  _describe 'pm-alf-console commands' _commands
}

_shtab_pm_alf_console_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--id[Unique gateway identifier, e.g. GW01]:GW_ID:"
  "--drop-copy[Enable drop-copy relay on startup (equivalent to sending DC\|STATE\=ON immediately after connecting). Default\: off. Can also be toggled at runtime with the DC\|STATE\=ON \/ DC\|STATE\=OFF command. See docs\/user-guide\/200-drop-copy.md]"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_alf_console_defaults_added=0


_shtab_pm_alf_console() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-alf-console'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_alf_console_defaults_added )); then
    if (( ${_shtab_pm_alf_console_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_alf_console_options[(I)${(q)remainder}*]} +          ${_shtab_pm_alf_console_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_alf_console_options+=(': :_shtab_pm_alf_console_commands' '*::: :->pm-alf-console')
    fi
    _shtab_pm_alf_console_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_alf_console_options

  case $state in
    pm-alf-console)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_alf_console-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_alf_console -N pm-alf-console
else
  # autoload from fpath, call function directly
  _shtab_pm_alf_console "$@"
fi

#compdef pm-alf-gwy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-alf-gwy (e.g. ~/.local/share/zsh_completion/_pm-alf-gwy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_alf_gwy_commands() {
  local _commands=(
    
  )
  _describe 'pm-alf-gwy commands' _commands
}

_shtab_pm_alf_gwy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--bind[TCP bind address override]:bind:"
  "--port[TCP bind port override]:port:"
  "--engine-host[Override engine host in ZMQ URLs (uses tcp\:\/\/\<host\>\:5555, tcp\:\/\/\<host\>\:5556, and tcp\:\/\/\<host\>\:5557)]:engine_host:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_alf_gwy_defaults_added=0


_shtab_pm_alf_gwy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-alf-gwy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_alf_gwy_defaults_added )); then
    if (( ${_shtab_pm_alf_gwy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_alf_gwy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_alf_gwy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_alf_gwy_options+=(': :_shtab_pm_alf_gwy_commands' '*::: :->pm-alf-gwy')
    fi
    _shtab_pm_alf_gwy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_alf_gwy_options

  case $state in
    pm-alf-gwy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_alf_gwy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_alf_gwy -N pm-alf-gwy
else
  # autoload from fpath, call function directly
  _shtab_pm_alf_gwy "$@"
fi

#compdef pm-api-gwy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-api-gwy (e.g. ~/.local/share/zsh_completion/_pm-api-gwy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_api_gwy_commands() {
  local _commands=(
    
  )
  _describe 'pm-api-gwy commands' _commands
}

_shtab_pm_api_gwy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--host[HTTP bind address]:ADDR:"
  "--port[HTTP listen port]:PORT:"
  "--instance[Named api_gateways entry to run when multiple API gateway processes are configured]:NAME:"
  "--engine-host[Override engine host in ZMQ URLs]:HOST:"
  "--stats-db[Path to stats.db]:PATH:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_api_gwy_defaults_added=0


_shtab_pm_api_gwy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-api-gwy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_api_gwy_defaults_added )); then
    if (( ${_shtab_pm_api_gwy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_api_gwy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_api_gwy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_api_gwy_options+=(': :_shtab_pm_api_gwy_commands' '*::: :->pm-api-gwy')
    fi
    _shtab_pm_api_gwy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_api_gwy_options

  case $state in
    pm-api-gwy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_api_gwy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_api_gwy -N pm-api-gwy
else
  # autoload from fpath, call function directly
  _shtab_pm_api_gwy "$@"
fi

#compdef pm-audit

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-audit (e.g. ~/.local/share/zsh_completion/_pm-audit).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_audit_commands() {
  local _commands=(
    
  )
  _describe 'pm-audit commands' _commands
}

_shtab_pm_audit_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--audit-log-file[Audit-trail log file path (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log)]:PATH:"
  {--terminal,-t}"[Also print each audit entry to stdout]"
  "--buffer-size[Number of messages to buffer before writing to disk (default\: 100)]:N:"
  "--flush-interval[Maximum seconds to wait before flushing buffer (default\: 10.0)]:SECONDS:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_defaults_added=0


_shtab_pm_audit() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-audit'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_audit_defaults_added )); then
    if (( ${_shtab_pm_audit_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_audit_options[(I)${(q)remainder}*]} +          ${_shtab_pm_audit_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_audit_options+=(': :_shtab_pm_audit_commands' '*::: :->pm-audit')
    fi
    _shtab_pm_audit_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_audit_options

  case $state in
    pm-audit)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_audit-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_audit -N pm-audit
else
  # autoload from fpath, call function directly
  _shtab_pm_audit "$@"
fi

#compdef pm-audit-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-audit-cli (e.g. ~/.local/share/zsh_completion/_pm-audit-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_audit_cli_commands() {
  local _commands=(
    events:'Search log entries by topic, gateway, symbol, and time range'
    gateways:'List gateways and summarise their audit trail activity'
    index:'Build or update the optional SQLite index for faster queries'
    orders:'Find order lifecycle events for specific order IDs or filters'
    stats:'Show summary statistics about audit log files'
    timeline:'Show raw chronological event stream for session replay'
    topics:'List topics present in logs with event counts'
    trades:'Find trade executions with filters'
  )
  _describe 'pm-audit-cli commands' _commands
}

_shtab_pm_audit_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--log-file[Primary audit log file (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log)]:PATH:"
  "--log-dir[Directory containing rotated log backups.]:PATH:"
  "--format[Output format\: table (default), json, or csv]:format:(table json csv)"
  "--no-header[Suppress header row for csv output]"
  "--use-index[Path to SQLite index file.]:PATH:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_defaults_added=0

_shtab_pm_audit_cli_events_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--topic[Topic prefix filter (e.g. order.fill)]:PREFIX:"
  "--gateway[Filter by gateway ID]:GW_ID:"
  "--symbol[Filter by symbol]:SYMBOL:"
  "--date[Restrict to a trading date]:YYYY-MM-DD:"
  "--from[Start of time range]:ISO_TS:"
  "--to[End of time range]:ISO_TS:"
  "--limit[Maximum rows (default\: 100)]:N:"
  "--reverse[Show newest first]"
  {-f,--follow}"[Watch for new events instead of exiting (Ctrl-C to stop)]"
  "--interval[Polling interval in seconds for --follow (default\: 1.0)]:SEC:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_events_defaults_added=0

_shtab_pm_audit_cli_gateways_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[Trading date]:YYYY-MM-DD:"
  "--from[Start of time range]:ISO_TS:"
  "--to[End of time range]:ISO_TS:"
  "--min-events[Minimum events to include a gateway]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_gateways_defaults_added=0

_shtab_pm_audit_cli_index_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--output[SQLite database path (default\: \<log-dir\>\/audit_index.db)]:PATH:"
  "--days[Index last N days only]:N:"
  "--from[Start of index range]:ISO_TS:"
  "--to[End of index range]:ISO_TS:"
  "--rebuild[Rebuild index from scratch]"
  "--incremental[Add only new entries since last index]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_index_defaults_added=0

_shtab_pm_audit_cli_orders_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "*--id[Order ID to search for (repeatable)]:ORDER_ID:"
  "--gateway[Filter by gateway ID]:GW_ID:"
  "--symbol[Filter by symbol]:SYMBOL:"
  "--date[Restrict to a trading date]:YYYY-MM-DD:"
  "--from[Start of time range]:ISO_TS:"
  "--to[End of time range]:ISO_TS:"
  "--limit[Maximum rows (default\: 100)]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_orders_defaults_added=0

_shtab_pm_audit_cli_stats_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--verbose[Show per-file breakdown]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_stats_defaults_added=0

_shtab_pm_audit_cli_timeline_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--from[Start time]:ISO_TS:"
  "--to[End time]:ISO_TS:"
  "--topic[Filter by topic prefix]:PREFIX:"
  "--gateway[Filter by gateway]:GW_ID:"
  "--symbol[Filter by symbol]:SYMBOL:"
  "--limit[Maximum events (default\: 500)]:N:"
  {-f,--follow}"[Watch for new events instead of exiting (Ctrl-C to stop)]"
  "--interval[Polling interval in seconds for --follow (default\: 1.0)]:SEC:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_timeline_defaults_added=0

_shtab_pm_audit_cli_topics_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[Restrict to a trading date]:YYYY-MM-DD:"
  "--from[Start of time range]:ISO_TS:"
  "--to[End of time range]:ISO_TS:"
  "--prefix[Filter topics by prefix (e.g. order.)]:PREFIX:"
  "--sort[Sort by event count (default) or alphabetically]:sort:(count alpha)"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_topics_defaults_added=0

_shtab_pm_audit_cli_trades_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--symbol[Filter by symbol]:SYMBOL:"
  "--gateway[Filter by buyer or seller gateway]:GW_ID:"
  "--buy-gateway[Filter by buyer gateway]:GW_ID:"
  "--sell-gateway[Filter by seller gateway]:GW_ID:"
  "--min-price[Minimum trade price]:PRICE:"
  "--max-price[Maximum trade price]:PRICE:"
  "--min-qty[Minimum trade quantity]:QTY:"
  "--date[Trading date]:YYYY-MM-DD:"
  "--from[Start of time range]:ISO_TS:"
  "--to[End of time range]:ISO_TS:"
  "--limit[Maximum rows (default\: 100)]:N:"
  "--reverse[Show newest first]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_cli_trades_defaults_added=0


_shtab_pm_audit_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-audit-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_audit_cli_defaults_added )); then
    if (( ${_shtab_pm_audit_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_audit_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_audit_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_audit_cli_options+=(': :_shtab_pm_audit_cli_commands' '*::: :->pm-audit-cli')
    fi
    _shtab_pm_audit_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_audit_cli_options

  case $state in
    pm-audit-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_audit_cli-$line[1]:"
      case $line[1] in
        events) _arguments -C -s $_shtab_pm_audit_cli_events_options ;;
        gateways) _arguments -C -s $_shtab_pm_audit_cli_gateways_options ;;
        index) _arguments -C -s $_shtab_pm_audit_cli_index_options ;;
        orders) _arguments -C -s $_shtab_pm_audit_cli_orders_options ;;
        stats) _arguments -C -s $_shtab_pm_audit_cli_stats_options ;;
        timeline) _arguments -C -s $_shtab_pm_audit_cli_timeline_options ;;
        topics) _arguments -C -s $_shtab_pm_audit_cli_topics_options ;;
        trades) _arguments -C -s $_shtab_pm_audit_cli_trades_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_audit_cli -N pm-audit-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_audit_cli "$@"
fi

#compdef pm-audit-replay

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-audit-replay (e.g. ~/.local/share/zsh_completion/_pm-audit-replay).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_audit_replay_commands() {
  local _commands=(
    anomalies:'The bug-hunting view (design section 9.6). Every finding from'
    digest:'A whole session or a whole day, collapsed (design section 9.4).'
    episodes:'A structured listing (design section 9.5): kind, anchor, actor,'
    index:'Materialise the episode model into SQLite (design section 9.7).'
    stats:'How the tool arrived at what it knows. On a log recorded since'
    story:'One episode and everything causally connected to it (section'
    stream:'Everything the exchange did, in causal order (section 9.2).'
  )
  _describe 'pm-audit-replay commands' _commands
}

_shtab_pm_audit_replay_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log). Rotated .1, .2.gz ... siblings are discovered automatically.]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s (default\: 2000\/5s)]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal, for a pager that renders it\: \`... --force-color \| less -R\`]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting (default\: 0.1s)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_defaults_added=0

_shtab_pm_audit_replay_anomalies_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log).]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal,]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting]:SECONDS:"
  "--severity[Report findings at this severity or worse (default\: info)]:severity:(error warn info)"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_anomalies_defaults_added=0

_shtab_pm_audit_replay_digest_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log).]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal,]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting]:SECONDS:"
  "--top[Show only the N most significant episodes]:N:"
  "--significance[What makes an episode significant]:significance:(anomalies notional qty duration)"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_digest_defaults_added=0

_shtab_pm_audit_replay_episodes_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log).]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal,]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting]:SECONDS:"
  "*--outcome[Restrict to episode outcomes, e.g. REJECTED (repeatable)]:OUTCOME:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_episodes_defaults_added=0

_shtab_pm_audit_replay_index_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log).]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal,]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting]:SECONDS:"
  "--stats[Report what the index holds once it is current]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_index_defaults_added=0

_shtab_pm_audit_replay_stats_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log).]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal,]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_stats_defaults_added=0

_shtab_pm_audit_replay_story_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log).]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal,]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting]:SECONDS:"
  "--order[Follow an order (an unambiguous prefix will do)]:ORDER_ID:"
  "--trade[Follow a trade and both its legs]:TRADE_ID:"
  "--quote[Follow a quote and its derived leg orders]:QUOTE_ID:"
  "--oco[Follow an OCO pair]:OCO_ID:"
  "--combo[Follow a combo and its legs]:COMBO_ID:"
  "--command[Follow a risk\/admin command and its effects]:COMMAND_ID:"
  "--client-tag[Follow by the client\'s own tag]:TAG:"
  "--chain[Follow a whole causal chain (the complete descent)]:ULID:"
  "--msg[Follow one message and what it caused]:ULID:"
  "--depth[Link-following depth (default\: 2)]:N:"
  "--strict-causality[Follow only RECORDED links, never an inferred one]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_story_defaults_added=0

_shtab_pm_audit_replay_stream_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--log-file[Audit log to read (default\: \<EDUMATCHER_DATA_DIR\>\/audit.log).]:PATH:"
  "--db[Episode index (default\: \<EDUMATCHER_DATA_DIR\>\/audit_replay.db)]:PATH:"
  "--no-index[Stream without building or reading an index]"
  "--rebuild[Rebuild the episode index before rendering]"
  "--from[Start of window (ISO-8601 or YYYY-MM-DD)]:ISO_TS:"
  "--to[End of window]:ISO_TS:"
  "--date[Shorthand for a whole UTC day]:YYYY-MM-DD:"
  "--last[Relative window, e.g. 15m, 2h, 1d]:DURATION:"
  "*--symbol[Restrict to one or more symbols (repeatable)]:SYMBOL:"
  "*--gateway[Restrict to one or more gateways (repeatable)]:GW_ID:"
  "*--kind[Restrict to episode kinds (repeatable)]:KIND:"
  "-q[Detail level 0\: episode outcomes only]"
  "*-v[Raise detail level\: -v, -vv, -vvv (see design section 8.1)]"
  "--format[Output format (default\: text)]:format:(text ndjson json markdown csv)"
  "--show-source[Append audit.log\:LINE to every narrated line]"
  "--show-units[Append unit provenance to every price]"
  "--explain[Show link evidence and confidence inline]"
  "--id-len[Order-id abbreviation (default\: 6)]:N|full:"
  "--actor-style[Actor naming\: id (default) or descriptive]:actor_style:(id descriptive)"
  "--tz[Render timestamps in this zone (default\: UTC)]:TZ:"
  "--reorder-window[Reorder buffer, e.g. 2000 or 5s]:SPEC:"
  "--no-color[Never emit ANSI colour, even to a terminal]"
  "--force-color[Emit ANSI colour even when stdout is not a terminal,]"
  "--strict[Report findings that are expected at a window edge (ARRIVAL_SEQ_GAP)]"
  "--clock-skew-warn[Engine\/receipt clock difference worth reporting]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_audit_replay_stream_defaults_added=0


_shtab_pm_audit_replay() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-audit-replay'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_audit_replay_defaults_added )); then
    if (( ${_shtab_pm_audit_replay_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_audit_replay_options[(I)${(q)remainder}*]} +          ${_shtab_pm_audit_replay_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_audit_replay_options+=(': :_shtab_pm_audit_replay_commands' '*::: :->pm-audit-replay')
    fi
    _shtab_pm_audit_replay_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_audit_replay_options

  case $state in
    pm-audit-replay)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_audit_replay-$line[1]:"
      case $line[1] in
        anomalies) _arguments -C -s $_shtab_pm_audit_replay_anomalies_options ;;
        digest) _arguments -C -s $_shtab_pm_audit_replay_digest_options ;;
        episodes) _arguments -C -s $_shtab_pm_audit_replay_episodes_options ;;
        index) _arguments -C -s $_shtab_pm_audit_replay_index_options ;;
        stats) _arguments -C -s $_shtab_pm_audit_replay_stats_options ;;
        story) _arguments -C -s $_shtab_pm_audit_replay_story_options ;;
        stream) _arguments -C -s $_shtab_pm_audit_replay_stream_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_audit_replay -N pm-audit-replay
else
  # autoload from fpath, call function directly
  _shtab_pm_audit_replay "$@"
fi

#compdef pm-balf-gwy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-balf-gwy (e.g. ~/.local/share/zsh_completion/_pm-balf-gwy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_balf_gwy_commands() {
  local _commands=(
    
  )
  _describe 'pm-balf-gwy commands' _commands
}

_shtab_pm_balf_gwy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--bind[TCP bind address override]:bind:"
  "--port[TCP bind port override]:port:"
  "--engine-host[Override engine host in ZMQ URLs (uses tcp\:\/\/\<host\>\:5555 and tcp\:\/\/\<host\>\:5556)]:engine_host:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_balf_gwy_defaults_added=0


_shtab_pm_balf_gwy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-balf-gwy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_balf_gwy_defaults_added )); then
    if (( ${_shtab_pm_balf_gwy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_balf_gwy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_balf_gwy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_balf_gwy_options+=(': :_shtab_pm_balf_gwy_commands' '*::: :->pm-balf-gwy')
    fi
    _shtab_pm_balf_gwy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_balf_gwy_options

  case $state in
    pm-balf-gwy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_balf_gwy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_balf_gwy -N pm-balf-gwy
else
  # autoload from fpath, call function directly
  _shtab_pm_balf_gwy "$@"
fi

#compdef pm-board

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-board (e.g. ~/.local/share/zsh_completion/_pm-board).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_board_commands() {
  local _commands=(
    
  )
  _describe 'pm-board commands' _commands
}

_shtab_pm_board_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  {--rows,-r}"[Max symbols (rows) per page (default 8)]:rows:"
  {--interval,-i}"[Auto-rotate interval in seconds (default 10)]:interval:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_board_defaults_added=0


_shtab_pm_board() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-board'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_board_defaults_added )); then
    if (( ${_shtab_pm_board_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_board_options[(I)${(q)remainder}*]} +          ${_shtab_pm_board_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_board_options+=(': :_shtab_pm_board_commands' '*::: :->pm-board')
    fi
    _shtab_pm_board_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_board_options

  case $state in
    pm-board)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_board-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_board -N pm-board
else
  # autoload from fpath, call function directly
  _shtab_pm_board "$@"
fi

#compdef pm-calf-spy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-calf-spy (e.g. ~/.local/share/zsh_completion/_pm-calf-spy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_calf_spy_commands() {
  local _commands=(
    
  )
  _describe 'pm-calf-spy commands' _commands
}

_shtab_pm_calf_spy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--host[pm-md-gwy TCP host (default\: 127.0.0.1)]:host:"
  "--port[pm-md-gwy TCP port (default\: 5570)]:port:"
  "--client-name[HELLO\|CLIENT\= identifier reported in gateway logs (default\: calf-spy-\<pid\>)]:NAME:"
  "--channels[Comma-separated channels to subscribe to, e.g. TOP,TRADE,CB. \'\*\' (default) subscribes to every channel the gateway advertises via WELCOME\|CH_SUPPORTED\= (falling back to TOP,TRADE,STATE if that field is absent).]:CH[,CH...]:"
  "--symbols[Comma-separated symbols to subscribe to, e.g. AAPL,MSFT. \'\*\' (default) requests the wildcard for every channel that allows it (TOP, TRADE, STATE, AUCTION)\; channels that reject SYM\=\* (INDEX, DEPTH, CB) will report an ERR\|CODE\=INVALID_SYMBOL for that channel alone rather than aborting the whole session -- pass explicit symbols to subscribe to those too.]:SYM[,SYM...]:"
  "--resume[Request single-stream replay on connect, e.g. TOP\:AAPL\:1042 (sends RESUME\|CH\=..\|SYM\=..\|LASTSEQ\=.. after the handshake). This flag resumes one stream\; live subscriptions from --channels\/--symbols are still applied afterwards.]:CH:SYM:LASTSEQ:"
  "--ping-interval[Send PING to the gateway every SECONDS seconds to avoid its idle timeout (default\: 60). The gateway replies PONG (hidden by default, see --show-heartbeats). Set to 0 to disable.]:SECONDS:"
  "--format[Output format (default\: human)]:format:(human json)"
  "--raw[Also echo the raw wire line under each formatted line (human format only\; ignored in json format, where the parsed record already carries every field verbatim)]"
  "--no-color[Disable ANSI colour even when stdout is a terminal]"
  "--show-heartbeats[Also print HB heartbeat lines and PONG keep-alive replies (suppressed by default to reduce noise\; they still keep the connection alive either way)]"
  "--count[Exit after N data-carrying lines (0 \= run until Ctrl-C, default). Heartbeats do not count towards N.]:N:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file. Note\: stdout is also used for --format json data output, so server (default) or file is recommended if a server isn\'t running]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_calf_spy_defaults_added=0


_shtab_pm_calf_spy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-calf-spy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_calf_spy_defaults_added )); then
    if (( ${_shtab_pm_calf_spy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_calf_spy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_calf_spy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_calf_spy_options+=(': :_shtab_pm_calf_spy_commands' '*::: :->pm-calf-spy')
    fi
    _shtab_pm_calf_spy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_calf_spy_options

  case $state in
    pm-calf-spy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_calf_spy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_calf_spy -N pm-calf-spy
else
  # autoload from fpath, call function directly
  _shtab_pm_calf_spy "$@"
fi

#compdef pm-clearing

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-clearing (e.g. ~/.local/share/zsh_completion/_pm-clearing).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_clearing_commands() {
  local _commands=(
    
  )
  _describe 'pm-clearing commands' _commands
}

_shtab_pm_clearing_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--datapath[Data directory or explicit .db file path]:PATH:"
  "--db-name[SQLite filename within data directory (default\: clearing.db)]:NAME:"
  "--flush-size[Max buffered trades before flush (1..100, default\: 100)]:N:"
  "--flush-interval[Max seconds between flushes (\>\=0.1, default\: 5.0)]:SEC:"
  "--print-every[Print P\&L summary every N trades (0 \= never, default\: 100)]:N:"
  "--retention-days[Delete trade_events rows older than N days on startup (default\: 90\; 0 \= disable pruning)]:N:"
  "--timezone[Exchange session timezone used to bucket trades into a trading day (IANA name, e.g. America\/New_York\; default\: UTC)]:TZ:"
  "--sql-trace[Log executed SQLite statements from the clearing writer connection]"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_defaults_added=0


_shtab_pm_clearing() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-clearing'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_clearing_defaults_added )); then
    if (( ${_shtab_pm_clearing_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_clearing_options[(I)${(q)remainder}*]} +          ${_shtab_pm_clearing_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_clearing_options+=(': :_shtab_pm_clearing_commands' '*::: :->pm-clearing')
    fi
    _shtab_pm_clearing_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_clearing_options

  case $state in
    pm-clearing)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_clearing-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_clearing -N pm-clearing
else
  # autoload from fpath, call function directly
  _shtab_pm_clearing "$@"
fi

#compdef pm-clearing-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-clearing-cli (e.g. ~/.local/share/zsh_completion/_pm-clearing-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_clearing_cli_commands() {
  local _commands=(
    daily:'Daily rollup summaries'
    dates:'Available trading dates'
    eod:'End-of-day sentinel events written by pm-clearing on system.eod'
    exposure:'Net/gross notional exposure'
    gateways:'List gateways with P&L totals'
    health:'DB metadata and row counts'
    pnl:'Realized/unrealized/total P&L'
    positions:'Current positions by gateway/symbol'
    prune:'Delete trade_events rows older than N days (default: 90) and VACUUM'
    reconcile:'Compare raw trade_events against gateway_daily_summary aggregates'
    sessions:'Gateway connection and disconnection history'
    symbols:'Symbol-level clearing totals'
    trades:'Raw trade events'
  )
  _describe 'pm-clearing-cli commands' _commands
}

_shtab_pm_clearing_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--datapath[Data directory or explicit .db file path]:PATH:"
  "--db-name[SQLite filename within data directory (default\: clearing.db)]:NAME:"
  "--format[Output format\: table, json, csv (default\: table)]:format:(table json csv)"
  "--no-header[Suppress header row for csv output]"
  "--raw-output[Show raw tick-unit values instead of normalized display values]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_defaults_added=0

_shtab_pm_clearing_cli_daily_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[YYYY-MM-DD]:YYYY-MM-DD:"
  "--to[YYYY-MM-DD]:YYYY-MM-DD:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_daily_defaults_added=0

_shtab_pm_clearing_cli_dates_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--from[YYYY-MM-DD]:YYYY-MM-DD:"
  "--to[YYYY-MM-DD]:YYYY-MM-DD:"
  "--with-totals[Include per-date quantity and net-amount totals]"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_dates_defaults_added=0

_shtab_pm_clearing_cli_eod_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--from[YYYY-MM-DD]:YYYY-MM-DD:"
  "--to[YYYY-MM-DD]:YYYY-MM-DD:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_eod_defaults_added=0

_shtab_pm_clearing_cli_exposure_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--sort[FIELD]:FIELD:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_exposure_defaults_added=0

_shtab_pm_clearing_cli_gateways_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_gateways_defaults_added=0

_shtab_pm_clearing_cli_health_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_health_defaults_added=0

_shtab_pm_clearing_cli_pnl_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_pnl_defaults_added=0

_shtab_pm_clearing_cli_positions_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_positions_defaults_added=0

_shtab_pm_clearing_cli_prune_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--days[Retention window in days (default\: 90)]:N:"
  "--dry-run[Show how many rows would be deleted without actually deleting]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_prune_defaults_added=0

_shtab_pm_clearing_cli_reconcile_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--from[YYYY-MM-DD]:YYYY-MM-DD:"
  "--to[YYYY-MM-DD]:YYYY-MM-DD:"
  "--retention-days[Ignore dates older than N days (match pm-clearing --retention-days) so pruned-raw days are not reported as false positives]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_reconcile_defaults_added=0

_shtab_pm_clearing_cli_sessions_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[Filter to one gateway]:GW_ID:"
  "--from[Inclusive start date (connect_date)]:YYYY-MM-DD:"
  "--to[Inclusive end date (connect_date)]:YYYY-MM-DD:"
  "--connected-only[Show only sessions without a recorded disconnect]"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_sessions_defaults_added=0

_shtab_pm_clearing_cli_symbols_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[YYYY-MM-DD]:YYYY-MM-DD:"
  "--to[YYYY-MM-DD]:YYYY-MM-DD:"
  "--sort[FIELD]:FIELD:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_symbols_defaults_added=0

_shtab_pm_clearing_cli_trades_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GW_ID]:GW_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[YYYY-MM-DD]:YYYY-MM-DD:"
  "--to[YYYY-MM-DD]:YYYY-MM-DD:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_clearing_cli_trades_defaults_added=0


_shtab_pm_clearing_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-clearing-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_clearing_cli_defaults_added )); then
    if (( ${_shtab_pm_clearing_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_clearing_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_clearing_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_clearing_cli_options+=(': :_shtab_pm_clearing_cli_commands' '*::: :->pm-clearing-cli')
    fi
    _shtab_pm_clearing_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_clearing_cli_options

  case $state in
    pm-clearing-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_clearing_cli-$line[1]:"
      case $line[1] in
        daily) _arguments -C -s $_shtab_pm_clearing_cli_daily_options ;;
        dates) _arguments -C -s $_shtab_pm_clearing_cli_dates_options ;;
        eod) _arguments -C -s $_shtab_pm_clearing_cli_eod_options ;;
        exposure) _arguments -C -s $_shtab_pm_clearing_cli_exposure_options ;;
        gateways) _arguments -C -s $_shtab_pm_clearing_cli_gateways_options ;;
        health) _arguments -C -s $_shtab_pm_clearing_cli_health_options ;;
        pnl) _arguments -C -s $_shtab_pm_clearing_cli_pnl_options ;;
        positions) _arguments -C -s $_shtab_pm_clearing_cli_positions_options ;;
        prune) _arguments -C -s $_shtab_pm_clearing_cli_prune_options ;;
        reconcile) _arguments -C -s $_shtab_pm_clearing_cli_reconcile_options ;;
        sessions) _arguments -C -s $_shtab_pm_clearing_cli_sessions_options ;;
        symbols) _arguments -C -s $_shtab_pm_clearing_cli_symbols_options ;;
        trades) _arguments -C -s $_shtab_pm_clearing_cli_trades_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_clearing_cli -N pm-clearing-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_clearing_cli "$@"
fi

#compdef pm-config-deploy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-config-deploy (e.g. ~/.local/share/zsh_completion/_pm-config-deploy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_config_deploy_commands() {
  local _commands=(
    
  )
  _describe 'pm-config-deploy commands' _commands
}

_shtab_pm_config_deploy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--example[Use a bundled example config instead of SOURCE, e.g. \'s3-basic\' for docs\/examples\/ref_data\/s3-basic-setup\/engine_config.yaml\; append \'-nomm\' for the no-market-maker-quotes variant, e.g. \'s3-basic-nomm\']:NAME:"
  "--check[Validate and compile, but install nothing]"
  "--show[Print the deployed configuration paths and exit]"
  ":Authored engine_config.yaml to validate, compile and install:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_config_deploy_defaults_added=0


_shtab_pm_config_deploy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-config-deploy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_config_deploy_defaults_added )); then
    if (( ${_shtab_pm_config_deploy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_config_deploy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_config_deploy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_config_deploy_options+=(': :_shtab_pm_config_deploy_commands' '*::: :->pm-config-deploy')
    fi
    _shtab_pm_config_deploy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_config_deploy_options

  case $state in
    pm-config-deploy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_config_deploy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_config_deploy -N pm-config-deploy
else
  # autoload from fpath, call function directly
  _shtab_pm_config_deploy "$@"
fi

#compdef pm-config-gen

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-config-gen (e.g. ~/.local/share/zsh_completion/_pm-config-gen).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_config_gen_commands() {
  local _commands=(
    
  )
  _describe 'pm-config-gen commands' _commands
}

_shtab_pm_config_gen_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--symbols[One or more symbols.]:SYM:"
  "--gateways[One or more gateway specs (ID\[\:ROLE\[\:DISCONNECT\]\]).]:GW_SPEC:"
  "*--gateway-smp[Gateway-level self-match-prevention default (NONE, CANCEL_AGGRESSOR, CANCEL_RESTING, CANCEL_BOTH), applied by the engine to any order, combo leg, or quote from GW_ID that doesn\'t specify its own SMP\=. GW_ID must be one of the IDs given to --gateways. Omit for a gateway to leave its default at NONE. Can be repeated.]:GW_ID:SMP_ACTION:"
  "*--symbol-opts[Per-symbol overrides. Can be repeated.]:SYMBOL:KEY=VALUE[,KEY=VALUE]:"
  "*--symbol-static-band[Per-symbol collar static band pct in (0,1). Can be repeated.]:SYM:PCT:"
  "*--symbol-dynamic-band[Per-symbol collar dynamic band pct in (0,1). Can be repeated.]:SYM:PCT:"
  "*--symbol-max-order-qty[Per-symbol order_limits.max_order_qty in shares (\> 0). Can be repeated.]:SYM:N:"
  "*--symbol-max-order-value[Per-symbol order_limits.max_order_value as notional (\> 0). Can be repeated.]:SYM:AMOUNT:"
  "*--symbol-risk-level[Per-symbol risk level key override. Can be repeated.]:SYM:LEVEL:"
  "*--outstanding-shares[Per-symbol outstanding shares (positive integer). Can be repeated.]:SYM:N:"
  "--sessions-enabled[Enable scheduler-driven sessions.]"
  "--no-sessions-enabled[Disable scheduler-driven sessions.]"
  "--snapshot-interval[Snapshot interval seconds (\> 0).]:SECS:"
  "--quote-history-maxlen[Max RECENT\/ALL quote-history entries kept per gateway (\> 0).]:N:"
  "--drop-copy-buffer-size[Drop-copy replay buffer size in messages (\> 0).]:N:"
  "--recent-trades-maxlen[Recent trades retained per book snapshot (\> 0).]:N:"
  "--depth-snapshot-tolerance-ticks[Depth snapshot window around last trade in ticks (\> 0).]:N:"
  "--no-collars[Set enforce_collars\: false.]"
  "--no-circuit-breakers[Set enforce_circuit_breakers\: false.]"
  "--static-band[DEFAULT static band pct in (0,1).]:PCT:"
  "--dynamic-band[DEFAULT dynamic band pct in (0,1).]:PCT:"
  "*--risk-level[Repeatable NAME\:STATIC_PCT\[\:DYNAMIC_PCT\]]:LEVEL_SPEC:"
  "--cb-levels[NAME\:SHIFT_PCT\[\:HALT_MINS\] entries. Omit HALT_MINS for a rest-of-day halt.]:CB_SPEC:"
  "--cb-window-ns[CB reference window nanoseconds.]:NS:"
  "--no-ace[Disable Automated Corridor Expansion. A halt then reopens at the equilibrium price whatever it is, with no corridor and no extensions.]"
  "--ace-initial-band[Reopening corridor half-width as a fraction of the reference price (default 0.1).]:PCT:"
  "--ace-expansions[WIDEN_PCT\:MINUTES rungs of the expansion ladder, in order. The last rung repeats indefinitely, which is what lets the corridor eventually contain any price. Default\: 0.10\:2 0.20\:5 (Nasdaq\'s ladder).]:EXP_SPEC:"
  "--ace-random-end-ns[Upper bound of the uniform random tail added to every call phase. 0 disables the random end, making reopen times predictable.]:NS:"
  "--ace-random-seed[Seed the random-end generator for reproducible demos. Omit for OS entropy, which is what a real venue wants.]:N:"
  "--mm-spread-ticks[Global MM max spread ticks.]:N:"
  "--mm-min-qty[Global MM min qty.]:N:"
  "--enforce-mm-obligations[Enable MM obligations globally.]"
  "--no-enforce-mm-obligations[Disable MM obligations globally.]"
  "--tick-decimals[Default symbol tick decimals.]:N:"
  "--seed-last-prices[Emit last_buy_price\/last_sell_price null placeholders.]"
  "--seed[Deterministic RNG seed for generated training values.]:N:"
  "--seed-mm-mid-range[Seed MM quotes from a random midpoint in the inclusive MIN\:MAX range, rounded to the symbol tick grid.]:MIN:MAX:"
  "--seed-last-prices-from-mm[When seeding MM quotes, set last_buy_price\/last_sell_price to the same midpoint used for the generated quote.]"
  "--no-mm-seed-quotes[Set require_mm_seed_quotes\: false. Allows a MARKET_MAKER gateway to be configured with no market_maker_quotes seed for any symbol, so the book starts genuinely empty. Without --seed-mm-mid-range, no market_maker_quotes entries are emitted at all.]"
  "--post-trade-gateway[Emit a top-level post_trade_gateway section for pm-ralf-gwy.]"
  "--post-trade-name[post_trade_gateway.name override.]:NAME:"
  "--post-trade-bind-address[post_trade_gateway.bind_address override.]:ADDR:"
  "--post-trade-port[post_trade_gateway.port override (\> 0).]:N:"
  "--post-trade-replay-retention-sec[post_trade_gateway.replay_retention_sec override (\> 0).]:N:"
  "--post-trade-heartbeat-interval-sec[post_trade_gateway.heartbeat_interval_sec override (\> 0).]:N:"
  "--post-trade-idle-timeout-sec[post_trade_gateway.idle_timeout_sec override (\> 0).]:N:"
  "--post-trade-max-client-queue[post_trade_gateway.max_client_queue override (\> 0).]:N:"
  "--post-trade-allowed-roles[post_trade_gateway.allowed_roles override (default\: CLEARING DROP_COPY AUDIT).]:ROLE:"
  "--market-data-gateway[Emit a top-level market_data_gateway section for pm-md-gwy.]"
  "--market-data-enabled[Set market_data_gateway.enabled\: true.]"
  "--market-data-disabled[Set market_data_gateway.enabled\: false.]"
  "--market-data-name[market_data_gateway.name override.]:NAME:"
  "--market-data-bind-address[market_data_gateway.bind_address override.]:ADDR:"
  "--market-data-port[market_data_gateway.port override (\> 0).]:N:"
  "--market-data-heartbeat-interval-sec[market_data_gateway.heartbeat_interval_sec override (\> 0).]:N:"
  "--market-data-idle-timeout-sec[market_data_gateway.idle_timeout_sec override (\> 0).]:N:"
  "--market-data-replay-window-sec[market_data_gateway.replay_window_sec override (\> 0).]:N:"
  "--market-data-max-symbols-per-client[market_data_gateway.max_symbols_per_client override (\> 0).]:N:"
  "--market-data-max-client-queue[market_data_gateway.max_client_queue override (\> 0).]:N:"
  "--market-data-depth-levels[market_data_gateway.depth_levels override (\> 0).]:N:"
  "--api-gateway[Emit a top-level api_gateways section for pm-api-gwy.]"
  "--api-gateway-name[Name for the generated api_gateways entry when using a single API gateway.]:NAME:"
  "*--api-gateway-instance[Repeatable named API gateway process. Generates credentials only for the listed gateway IDs and optionally overrides port. Use NAME\:\:PORT for an identity-free read-only gateway.]:NAME:GATEWAY[,GATEWAY...][:PORT]:"
  "--api-gateway-enabled[Set api_gateway.enabled\: true.]"
  "--api-gateway-disabled[Set api_gateway.enabled\: false.]"
  "--api-gateway-host[api_gateway.host override.]:ADDR:"
  "--api-gateway-port[api_gateway.port override (\> 0).]:N:"
  "--api-gateway-swagger-enabled[Set api_gateway.swagger_enabled\: true.]"
  "--api-gateway-swagger-disabled[Set api_gateway.swagger_enabled\: false.]"
  "--api-gateway-log-level[api_gateway.log_level override.]:api_gateway_log_level:(debug info warning error)"
  "--api-gateway-stats-db[api_gateway.stats_db override.]:PATH:"
  "*--api-key[Explicit api_gateways credential for single-instance generation. Use GATEWAY_ID\=null for a read-only market-data key. Can be repeated.]:KEY:GATEWAY_ID[:DESCRIPTION]:"
  "--api-gateway-generate-keys[Generate one API key for each configured ALF gateway when api_gateway is emitted.]"
  "--no-api-gateway-generate-keys[Do not auto-generate API keys for ALF gateways.]"
  "--api-gateway-readonly-key[Generate an additional read-only API key with gateway_id\: null.]"
  "--api-gateway-rate-limit-writes-per-second[api_gateway.rate_limit.writes_per_second override (\> 0).]:N:"
  "--api-gateway-rate-limit-burst[api_gateway.rate_limit.burst override (\> 0).]:N:"
  "--api-gateway-engine-auth-sec[api_gateway.timeouts.engine_auth_sec override (\> 0).]:SECS:"
  "--api-gateway-engine-reply-sec[api_gateway.timeouts.engine_reply_sec override (\> 0).]:SECS:"
  "--api-gateway-wait-ack-sec[api_gateway.timeouts.wait_ack_sec override (\> 0).]:SECS:"
  "--api-gateway-order-retention-sec[api_gateway.order_retention_sec override (\>\= 0). Seconds a terminal order stays in the gateway\'s in-memory cache. 0 disables eviction.]:SECS:"
  "--balf-gateway[Emit a top-level balf_gateway section for pm-balf-gwy.]"
  "--balf-name[balf_gateway.name override.]:NAME:"
  "--balf-bind-address[balf_gateway.bind_address override.]:ADDR:"
  "--balf-port[balf_gateway.port override (\> 0).]:N:"
  "--balf-heartbeat-interval-sec[balf_gateway.heartbeat_interval_sec override (\> 0).]:N:"
  "--balf-heartbeat-timeout-sec[balf_gateway.heartbeat_timeout_sec override (\> 0).]:N:"
  "--balf-idle-timeout-sec[balf_gateway.idle_timeout_sec override (\> 0).]:N:"
  "--balf-auth-timeout-sec[balf_gateway.auth_timeout_sec override (\> 0).]:N:"
  "--balf-max-connections[balf_gateway.max_connections override (\> 0).]:N:"
  "--balf-max-client-queue[balf_gateway.max_client_queue override (\> 0).]:N:"
  "--balf-max-messages-per-second[balf_gateway.max_messages_per_second override (\> 0).]:N:"
  "--balf-max-errors-before-disconnect[balf_gateway.max_errors_before_disconnect override (\> 0).]:N:"
  "--balf-error-window-sec[balf_gateway.error_window_sec override (\> 0).]:N:"
  "--balf-duplicate-session-policy[balf_gateway.duplicate_session_policy override.]:balf_duplicate_session_policy:(REJECT_NEW EVICT_OLD)"
  "--dc-gateway[Emit a top-level dc_gateway section for pm-dc-gwy.]"
  "--dc-name[dc_gateway.name override.]:NAME:"
  "--dc-bind-address[dc_gateway.bind_address override.]:ADDR:"
  "--dc-port[dc_gateway.port override (\> 0).]:N:"
  "--dc-heartbeat-interval-sec[dc_gateway.heartbeat_interval_sec override (\> 0).]:N:"
  "--dc-idle-timeout-sec[dc_gateway.idle_timeout_sec override (\> 0).]:N:"
  "--dc-max-client-queue[dc_gateway.max_client_queue override (\> 0).]:N:"
  "--log-server[Emit a top-level log_server section for pm-log-srv.]"
  "--log-server-enabled[Set log_server.enabled\: true.]"
  "--log-server-disabled[Set log_server.enabled\: false.]"
  "--log-server-name[log_server.name override.]:NAME:"
  "--log-server-bind-address[log_server.bind_address override.]:ADDR:"
  "--log-server-port[log_server.port override (\> 0).]:N:"
  "--log-server-db-path[log_server.db_path override.]:PATH:"
  "--log-server-retention-days[log_server.retention_days override (\>\= 0\; 0 means unbounded retention, same as omitting the field with retention disabled).]:N:"
  "--log-server-max-message-bytes[log_server.max_message_bytes override (\> 0).]:N:"
  "--log-server-max-client-queue[log_server.max_client_queue override (\> 0).]:N:"
  "--log-server-write-batch-size[log_server.write_batch_size override (\> 0).]:N:"
  "--log-server-write-batch-interval-ms[log_server.write_batch_interval_ms override (\> 0).]:N:"
  "--log-server-heartbeat-interval-sec[log_server.heartbeat_interval_sec override (\> 0).]:N:"
  "--log-server-pubsub-enabled[Set log_server.pubsub_enabled\: true (LALF-PS log distribution).]"
  "--log-server-pubsub-disabled[Set log_server.pubsub_enabled\: false (bind no ZeroMQ sockets).]"
  "--log-server-pub-port[log_server.pub_port override \— LALF-PS ZeroMQ PUB port (\> 0).]:N:"
  "--log-server-pull-port[log_server.pull_port override \— LALF-PS ZeroMQ PULL port (\> 0).]:N:"
  "--log-server-lease-sec[log_server.lease_sec override (\> 0) \— subscription lease TTL\; a subscriber that stops renewing is reaped after this long.]:N:"
  "--log-server-max-lease-sec[log_server.max_lease_sec override (\>\= lease_sec).]:N:"
  "--log-server-max-subscribers[log_server.max_subscribers override (\> 0).]:N:"
  "--log-server-notify-interval-ms[log_server.notify_interval_ms override (\> 0).]:N:"
  "--log-server-backfill-chunk-rows[log_server.backfill_chunk_rows override (\> 0).]:N:"
  "--log-server-max-backfill-minutes[log_server.max_backfill_minutes override (\> 0).]:N:"
  "--log-server-max-backfill-rows[log_server.max_backfill_rows override (\> 0).]:N:"
  "--log-server-max-pending-rows[log_server.max_pending_rows override (\> 0).]:N:"
  "--log-server-pub-sndhwm[log_server.pub_sndhwm override (\> 0).]:N:"
  "--schedule[Force emit schedule section.]"
  "--no-schedule[Suppress schedule section.]"
  "--pre-open[HH\:MM]:HH:MM:"
  "--opening-auction[HH\:MM]:HH:MM:"
  "--continuous[HH\:MM]:HH:MM:"
  "--closing-auction[HH\:MM]:HH:MM:"
  "--closing-end[HH\:MM]:HH:MM:"
  "--weekend[Also emit a weekend\: block (Sat\+Sun) using the --weekend-\* times below. Without this flag, weekends are CLOSED.]"
  "--weekend-pre-open[HH\:MM]:HH:MM:"
  "--weekend-opening-auction[HH\:MM]:HH:MM:"
  "--weekend-continuous[HH\:MM]:HH:MM:"
  "--weekend-closing-auction[HH\:MM]:HH:MM:"
  "--weekend-closing-end[HH\:MM]:HH:MM:"
  "--holidays[Also emit a holidays\: block using the --holidays-\* times below. Without this flag, bank holidays are CLOSED.]"
  "--no-holidays[Suppress the holidays\: block (the default).]"
  "--holidays-pre-open[HH\:MM]:HH:MM:"
  "--holidays-opening-auction[HH\:MM]:HH:MM:"
  "--holidays-continuous[HH\:MM]:HH:MM:"
  "--holidays-closing-auction[HH\:MM]:HH:MM:"
  "--holidays-closing-end[HH\:MM]:HH:MM:"
  "--country[Country pm-scheduler uses for its bank-holiday\/weekend calendar (name, e.g. \"Sweden\", or ISO 3166-1 alpha-2 code, e.g. \"SE\"). Defaults to \"Sweden\" when omitted.]:COUNTRY:"
  "*--index[Define an index (repeatable, up to 5). ID is alphanumeric\; DESCRIPTION is optional text after the first colon.]:ID[:DESCRIPTION]:"
  "*--index-constituents[Constituent symbols for an index. Can be repeated.]:ID:SYM[,SYM,...]:"
  "*--index-base-value[Override base_value for an index (default\: 1000.0). Can be repeated.]:ID:VALUE:"
  "*--index-interval[Override publish_interval_sec for an index (default\: 1.0). Can be repeated.]:ID:SECS:"
  "*--index-history-file[Override history_file path for an index. Can be repeated.]:ID:PATH:"
  "*--index-state-file[Override state_file path for an index. Can be repeated.]:ID:PATH:"
  "*--combo[Seed a market-maker combo order\: ID\:TYPE\:TIF\:SYM\/SIDE\/TYPE\/QTY\[\/PRICE\[\/STOP\[\/SMP\]\]\],...]:COMBO_SPEC:"
  "--output[Output file path.]:FILE:"
  "--force[Overwrite existing output file.]"
  "--dry-run[Print only, do not write.]"
  "--comment-default-config-fields[Add a header comment block listing defaultable engine_config fields and their default values.]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_config_gen_defaults_added=0


_shtab_pm_config_gen() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-config-gen'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_config_gen_defaults_added )); then
    if (( ${_shtab_pm_config_gen_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_config_gen_options[(I)${(q)remainder}*]} +          ${_shtab_pm_config_gen_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_config_gen_options+=(': :_shtab_pm_config_gen_commands' '*::: :->pm-config-gen')
    fi
    _shtab_pm_config_gen_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_config_gen_options

  case $state in
    pm-config-gen)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_config_gen-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_config_gen -N pm-config-gen
else
  # autoload from fpath, call function directly
  _shtab_pm_config_gen "$@"
fi

#compdef pm-config-show

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-config-show (e.g. ~/.local/share/zsh_completion/_pm-config-show).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_config_show_commands() {
  local _commands=(
    
  )
  _describe 'pm-config-show commands' _commands
}

_shtab_pm_config_show_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  {-f,--file}"[config file to read (default\: \<EDUMATCHER_DATA_DIR\>\/ref_data\/engine_config.yaml)]:YAML:"
  {-m,--density}"[pack more information in\; bare -m means 1]:{1,2}:(1 2)"
  {-a,--all}"[show everything, including unmasked API keys]"
  "--format[output format (default\: terminal)]:output_format:(terminal pdf)"
  {-o,--output}"[destination for --format pdf]:FILE:"
  "--no-color[disable ANSI colour]"
  "--ascii[ASCII box drawing (auto on non-UTF-8 terminals)]"
  "--width[force render width (testing \/ piping)]:width:"
  "--height[force render height (testing \/ piping)]:height:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_config_show_defaults_added=0


_shtab_pm_config_show() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-config-show'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_config_show_defaults_added )); then
    if (( ${_shtab_pm_config_show_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_config_show_options[(I)${(q)remainder}*]} +          ${_shtab_pm_config_show_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_config_show_options+=(': :_shtab_pm_config_show_commands' '*::: :->pm-config-show')
    fi
    _shtab_pm_config_show_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_config_show_options

  case $state in
    pm-config-show)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_config_show-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_config_show -N pm-config-show
else
  # autoload from fpath, call function directly
  _shtab_pm_config_show "$@"
fi

#compdef pm-cverifier

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-cverifier (e.g. ~/.local/share/zsh_completion/_pm-cverifier).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_cverifier_commands() {
  local _commands=(
    
  )
  _describe 'pm-cverifier commands' _commands
}

_shtab_pm_cverifier_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--format[Output format (default\: text)]:output_format:(text json)"
  "--level[Minimum severity to show (default\: info)]:level:(info warn error)"
  "--no-color[Disable ANSI color in text output]"
  "--strict[Treat warnings as errors for CI exit-code purposes]"
  ":Path to engine_config.yaml:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_cverifier_defaults_added=0


_shtab_pm_cverifier() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-cverifier'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_cverifier_defaults_added )); then
    if (( ${_shtab_pm_cverifier_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_cverifier_options[(I)${(q)remainder}*]} +          ${_shtab_pm_cverifier_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_cverifier_options+=(': :_shtab_pm_cverifier_commands' '*::: :->pm-cverifier')
    fi
    _shtab_pm_cverifier_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_cverifier_options

  case $state in
    pm-cverifier)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_cverifier-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_cverifier -N pm-cverifier
else
  # autoload from fpath, call function directly
  _shtab_pm_cverifier "$@"
fi

#compdef pm-dc-gwy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-dc-gwy (e.g. ~/.local/share/zsh_completion/_pm-dc-gwy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_dc_gwy_commands() {
  local _commands=(
    
  )
  _describe 'pm-dc-gwy commands' _commands
}

_shtab_pm_dc_gwy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--bind[TCP bind address override]:bind:"
  "--port[TCP bind port override]:port:"
  "--engine-dc-pub[Engine drop-copy PUB socket address (default\: value from config, then tcp\:\/\/127.0.0.1\:5557)]:engine_dc_pub:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_dc_gwy_defaults_added=0


_shtab_pm_dc_gwy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-dc-gwy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_dc_gwy_defaults_added )); then
    if (( ${_shtab_pm_dc_gwy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_dc_gwy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_dc_gwy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_dc_gwy_options+=(': :_shtab_pm_dc_gwy_commands' '*::: :->pm-dc-gwy')
    fi
    _shtab_pm_dc_gwy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_dc_gwy_options

  case $state in
    pm-dc-gwy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_dc_gwy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_dc_gwy -N pm-dc-gwy
else
  # autoload from fpath, call function directly
  _shtab_pm_dc_gwy "$@"
fi

#compdef pm-dc-spy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-dc-spy (e.g. ~/.local/share/zsh_completion/_pm-dc-spy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_dc_spy_commands() {
  local _commands=(
    
  )
  _describe 'pm-dc-spy commands' _commands
}

_shtab_pm_dc_spy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--host[Drop-copy PUB socket host (default\: 127.0.0.1)]:host:"
  "--port[Drop-copy PUB socket port (default\: 5557)]:port:"
  "--gateway[Only show fills for this gateway (subscribes to the drop_copy.event.\<GW_ID\> topic). Default\: all gateways (drop_copy.event. prefix). Note the drop-copy socket performs no entitlement checks -- any gateway\'s fills can be requested.]:GW_ID:"
  "--replay-of[Also subscribe to drop_copy.replay.\<RECIPIENT_ID\>, the topic DropCopyPublisher.replay() publishes on when a recipient\'s replay is requested programmatically. Replay lines are tagged REPLAY instead of FILL. Useful for observing replay() calls made by tests or embedded consumers\; there is no wire protocol to trigger a replay from pm-dc-spy itself (see docs\/user-guide\/200-drop-copy.md).]:RECIPIENT_ID:"
  "--format[Output format (default\: human)]:format:(human json)"
  "--raw[Also echo the raw topic \+ JSON payload under each formatted line (human format only\; ignored in json format, where the record already carries every field verbatim)]"
  "--no-color[Disable ANSI colour even when stdout is a terminal]"
  "--count[Exit after N messages (0 \= run until Ctrl-C, default).]:N:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file. Note\: stdout is also used for --format json data output, so server (default) or file is recommended if a server isn\'t running]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_dc_spy_defaults_added=0


_shtab_pm_dc_spy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-dc-spy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_dc_spy_defaults_added )); then
    if (( ${_shtab_pm_dc_spy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_dc_spy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_dc_spy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_dc_spy_options+=(': :_shtab_pm_dc_spy_commands' '*::: :->pm-dc-spy')
    fi
    _shtab_pm_dc_spy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_dc_spy_options

  case $state in
    pm-dc-spy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_dc_spy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_dc_spy -N pm-dc-spy
else
  # autoload from fpath, call function directly
  _shtab_pm_dc_spy "$@"
fi

#compdef pm-engine

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-engine (e.g. ~/.local/share/zsh_completion/_pm-engine).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_engine_commands() {
  local _commands=(
    
  )
  _describe 'pm-engine commands' _commands
}

_shtab_pm_engine_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_engine_defaults_added=0


_shtab_pm_engine() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-engine'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_engine_defaults_added )); then
    if (( ${_shtab_pm_engine_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_engine_options[(I)${(q)remainder}*]} +          ${_shtab_pm_engine_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_engine_options+=(': :_shtab_pm_engine_commands' '*::: :->pm-engine')
    fi
    _shtab_pm_engine_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_engine_options

  case $state in
    pm-engine)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_engine-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_engine -N pm-engine
else
  # autoload from fpath, call function directly
  _shtab_pm_engine "$@"
fi

#compdef pm-help

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-help (e.g. ~/.local/share/zsh_completion/_pm-help).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_help_commands() {
  local _commands=(
    
  )
  _describe 'pm-help commands' _commands
}

_shtab_pm_help_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--format[table \= UTF-8 box-drawing (default)\; text \= plain aligned columns]:output_format:(table text)"
  {-v,--verbose}"[show extra detail (and an example, where one exists) for each command]"
  "--no-color[disable ANSI colour]"
  "--completion[print the tab-completion script for every pm-\* command and exit]:SHELL:(bash zsh)"
  ":a pm-\* command name (e.g. pm-viewer) to show its full man page:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_help_defaults_added=0


_shtab_pm_help() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-help'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_help_defaults_added )); then
    if (( ${_shtab_pm_help_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_help_options[(I)${(q)remainder}*]} +          ${_shtab_pm_help_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_help_options+=(': :_shtab_pm_help_commands' '*::: :->pm-help')
    fi
    _shtab_pm_help_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_help_options

  case $state in
    pm-help)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_help-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_help -N pm-help
else
  # autoload from fpath, call function directly
  _shtab_pm_help "$@"
fi

#compdef pm-index

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-index (e.g. ~/.local/share/zsh_completion/_pm-index).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_index_commands() {
  local _commands=(
    
  )
  _describe 'pm-index commands' _commands
}

_shtab_pm_index_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--reset[Ignore\/delete persisted index state and initialise from config]"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_defaults_added=0


_shtab_pm_index() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-index'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_index_defaults_added )); then
    if (( ${_shtab_pm_index_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_index_options[(I)${(q)remainder}*]} +          ${_shtab_pm_index_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_index_options+=(': :_shtab_pm_index_commands' '*::: :->pm-index')
    fi
    _shtab_pm_index_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_index_options

  case $state in
    pm-index)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_index-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_index -N pm-index
else
  # autoload from fpath, call function directly
  _shtab_pm_index "$@"
fi

#compdef pm-index-admin-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-index-admin-cli (e.g. ~/.local/share/zsh_completion/_pm-index-admin-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_index_admin_cli_commands() {
  local _commands=(
    add:'Add a new constituent'
    delist:'Remove a constituent'
    dividend:'Apply a cash dividend adjustment'
    history:'Show recent structural/corp-action history for an index'
    shares:'Set shares outstanding (covers both issuances and buy-backs)'
    split:'Apply a stock split (or reverse split)'
  )
  _describe 'pm-index-admin-cli commands' _commands
}

_shtab_pm_index_admin_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--id[Gateway ID used as the ack-routing key. Not authenticated by pm-index (its PULL socket has no auth) \— any non-empty value works, but a real gateway_id makes acks easier to correlate.]:GW_ID:"
  "--push[pm-index PULL socket address (default\: tcp\:\/\/127.0.0.1\:5559)]:ADDR:"
  "--sub[pm-index PUB socket address (default\: tcp\:\/\/127.0.0.1\:5558)]:ADDR:"
  "--timeout[Ack timeout in milliseconds (default\: 3000)]:MS:"
  "--dry-run[Validate and print the outbound payload\; do not send it.]"
  {-y,--yes}"[Skip the interactive confirmation prompt for mutating commands.]"
  "--format[Output format\: table (default, human-readable) or json.]:format:(table json)"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_admin_cli_defaults_added=0

_shtab_pm_index_admin_cli_add_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--index[Index ID]:INDEX_ID:"
  "--sym[Symbol to add]:SYMBOL:"
  "--shares[Initial shares outstanding. Must be \> 0.]:N:"
  "--price[Initial reference price. Must be \> 0.]:PRICE:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_admin_cli_add_defaults_added=0

_shtab_pm_index_admin_cli_delist_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--index[Index ID]:INDEX_ID:"
  "--sym[Symbol to delist]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_admin_cli_delist_defaults_added=0

_shtab_pm_index_admin_cli_dividend_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--index[Index ID]:INDEX_ID:"
  "--sym[Constituent symbol]:SYMBOL:"
  "--amount[Dividend per share, in price units. Must be \> 0.]:AMOUNT:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_admin_cli_dividend_defaults_added=0

_shtab_pm_index_admin_cli_history_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--index[Index ID]:INDEX_ID:"
  "--from[Start of range as a Unix timestamp (default\: 24h ago).]:UNIX_TS:"
  "--to[End of range as a Unix timestamp (default\: now).]:UNIX_TS:"
  "--types[Comma-separated filter\: INIT, CORP_ACTION, DELIST, ADD_CONSTITUENT (default\: all).]:TYPE,TYPE,...:"
  "--limit[Maximum rows shown (default\: 50).]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_admin_cli_history_defaults_added=0

_shtab_pm_index_admin_cli_shares_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--index[Index ID]:INDEX_ID:"
  "--sym[Constituent symbol]:SYMBOL:"
  "--new-shares[New absolute total shares outstanding.]:N:"
  "--delta[Signed change applied to the constituent\'s last known shares outstanding (negative for a buy-back, positive for an issuance). Resolved via \'history\' before sending\; the computed absolute value is shown in the confirmation prompt.]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_admin_cli_shares_defaults_added=0

_shtab_pm_index_admin_cli_split_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--index[Index ID]:INDEX_ID:"
  "--sym[Constituent symbol]:SYMBOL:"
  "--ratio[Split ratio, e.g. 4\:1 for a 4-for-1 split, 1\:10 for a 1-for-10 reverse split]:N:M:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_admin_cli_split_defaults_added=0


_shtab_pm_index_admin_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-index-admin-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_index_admin_cli_defaults_added )); then
    if (( ${_shtab_pm_index_admin_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_index_admin_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_index_admin_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_index_admin_cli_options+=(': :_shtab_pm_index_admin_cli_commands' '*::: :->pm-index-admin-cli')
    fi
    _shtab_pm_index_admin_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_index_admin_cli_options

  case $state in
    pm-index-admin-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_index_admin_cli-$line[1]:"
      case $line[1] in
        add) _arguments -C -s $_shtab_pm_index_admin_cli_add_options ;;
        delist) _arguments -C -s $_shtab_pm_index_admin_cli_delist_options ;;
        dividend) _arguments -C -s $_shtab_pm_index_admin_cli_dividend_options ;;
        history) _arguments -C -s $_shtab_pm_index_admin_cli_history_options ;;
        shares) _arguments -C -s $_shtab_pm_index_admin_cli_shares_options ;;
        split) _arguments -C -s $_shtab_pm_index_admin_cli_split_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_index_admin_cli -N pm-index-admin-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_index_admin_cli "$@"
fi

#compdef pm-index-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-index-cli (e.g. ~/.local/share/zsh_completion/_pm-index-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_index_cli_commands() {
  local _commands=(
    events:'Show structural events: INIT, CORP_ACTION, ADD_CONSTITUENT, DELIST.'
    indices:'List indices from --config, or the deployed exchange'"'"'s compiled configuration if --config is omitted.'
  )
  _describe 'pm-index-cli commands' _commands
}

_shtab_pm_index_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  {--config,-c}"[Path to an engine_config.yaml to query instead of the]:PATH:"
  "--data-dir[Directory containing history files (default\: data\/indexes).]:DIR:"
  "--format[Output format\: table (default), json, or csv.]:format:(table json csv)"
  "--no-header[Suppress header row (csv output only).]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_cli_defaults_added=0

_shtab_pm_index_cli_events_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "*"{--index,-i}"[Index ID to query (repeatable).]:ID:"
  "*"{--type,-t}"[Filter to one event type (repeatable).]:TYPE:"
  "--days[Return records from the last N days.]:N:"
  "--from[Start of time range\: YYYY-MM-DD or ISO-8601 (e.g. 2026-06-14T09\:00\:00\+00\:00).]:DATE_OR_TS:"
  "--to[End of time range\: YYYY-MM-DD or ISO-8601. Defaults to now.]:DATE_OR_TS:"
  "--limit[Maximum rows per index (default\: 1000).]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_cli_events_defaults_added=0

_shtab_pm_index_cli_indices_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_index_cli_indices_defaults_added=0


_shtab_pm_index_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-index-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_index_cli_defaults_added )); then
    if (( ${_shtab_pm_index_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_index_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_index_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_index_cli_options+=(': :_shtab_pm_index_cli_commands' '*::: :->pm-index-cli')
    fi
    _shtab_pm_index_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_index_cli_options

  case $state in
    pm-index-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_index_cli-$line[1]:"
      case $line[1] in
        events) _arguments -C -s $_shtab_pm_index_cli_events_options ;;
        indices) _arguments -C -s $_shtab_pm_index_cli_indices_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_index_cli -N pm-index-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_index_cli "$@"
fi

#compdef pm-log-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-log-cli (e.g. ~/.local/share/zsh_completion/_pm-log-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_log_cli_commands() {
  local _commands=(
    diagnose:'Rule-based troubleshooting report'
    processes:'List connected/recently-connected pm-* processes'
    prune:'Manually prune log_events older than N days'
    query:'Filtered historical search'
    stats:'Server + database health summary'
    tail:'Follow new log_events rows in real time'
  )
  _describe 'pm-log-cli commands' _commands
}

_shtab_pm_log_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--db[SQLite database path (default\: \<EDUMATCHER_DATA_DIR\>\/log.db)]:PATH:"
  "--format[Output format\: human (default) or json]:format:(human json)"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_cli_defaults_added=0

_shtab_pm_log_cli_diagnose_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--since[Restrict to events since this time]:ISO_TS:"
  "--process[Restrict to a specific process]:NAME:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_cli_diagnose_defaults_added=0

_shtab_pm_log_cli_processes_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--active[Only currently-connected sessions]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_cli_processes_defaults_added=0

_shtab_pm_log_cli_prune_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--days[Delete rows older than N days (default\: 30)]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_cli_prune_defaults_added=0

_shtab_pm_log_cli_query_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--process[Filter by process name]:NAME:"
  "--level[Filter by one or more levels]:LEVEL[,LEVEL...]:"
  "--logger[SQL LIKE pattern on logger]:PATTERN:"
  "--since[Start of time range]:ISO_TS:"
  "--until[End of time range]:ISO_TS:"
  "--grep[Case-insensitive substring search]:TEXT:"
  "--has-exception[Only rows with an exception]"
  "--limit[Maximum rows (default\: 500)]:N:"
  "--reverse[Oldest-first instead of newest-first]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_cli_query_defaults_added=0

_shtab_pm_log_cli_stats_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_cli_stats_defaults_added=0

_shtab_pm_log_cli_tail_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--process[Filter by process name]:NAME:"
  "--level[Filter by one or more levels]:LEVEL[,LEVEL...]:"
  "--logger[SQL LIKE pattern on logger]:PATTERN:"
  "--interval[Polling interval in seconds (default\: 1.0)]:SEC:"
  "--before[Show this many existing rows before switching to live tail (1-999). Shorthand\: -NNN, e.g. -50.]:NNN:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_cli_tail_defaults_added=0


_shtab_pm_log_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-log-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_log_cli_defaults_added )); then
    if (( ${_shtab_pm_log_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_log_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_log_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_log_cli_options+=(': :_shtab_pm_log_cli_commands' '*::: :->pm-log-cli')
    fi
    _shtab_pm_log_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_log_cli_options

  case $state in
    pm-log-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_log_cli-$line[1]:"
      case $line[1] in
        diagnose) _arguments -C -s $_shtab_pm_log_cli_diagnose_options ;;
        processes) _arguments -C -s $_shtab_pm_log_cli_processes_options ;;
        prune) _arguments -C -s $_shtab_pm_log_cli_prune_options ;;
        query) _arguments -C -s $_shtab_pm_log_cli_query_options ;;
        stats) _arguments -C -s $_shtab_pm_log_cli_stats_options ;;
        tail) _arguments -C -s $_shtab_pm_log_cli_tail_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_log_cli -N pm-log-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_log_cli "$@"
fi

#compdef pm-log-srv

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-log-srv (e.g. ~/.local/share/zsh_completion/_pm-log-srv).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_log_srv_commands() {
  local _commands=(
    
  )
  _describe 'pm-log-srv commands' _commands
}

_shtab_pm_log_srv_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--host[TCP bind address override]:host:"
  "--port[TCP bind port override]:port:"
  "--db[SQLite database path override (default\: from config or data\/log.db)]:PATH:"
  "--retention-days[Prune log_events rows older than N days (default\: 30). Pass 0 to disable pruning (unbounded retention).]:N:"
  "--max-message-bytes[Maximum LOG payload size before truncation (default\: 65536)]:N:"
  "--pub-port[LALF-PS ZeroMQ PUB bind port for log distribution (default\: 5601)]:PORT:"
  "--pull-port[LALF-PS ZeroMQ PULL bind port for subscriber control (default\: 5602)]:PORT:"
  "--no-pubsub[Disable the LALF-PS interface entirely (bind no ZeroMQ sockets)]"
  "--lease-sec[Subscription lease TTL in seconds\; a subscriber that stops sending log.renew is reaped after this long (default\: 30)]:N:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: stdout (default) or file. Never \'server\' \— pm-log-srv must not depend on itself over the network]:log_target:(stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_log_srv_defaults_added=0


_shtab_pm_log_srv() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-log-srv'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_log_srv_defaults_added )); then
    if (( ${_shtab_pm_log_srv_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_log_srv_options[(I)${(q)remainder}*]} +          ${_shtab_pm_log_srv_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_log_srv_options+=(': :_shtab_pm_log_srv_commands' '*::: :->pm-log-srv')
    fi
    _shtab_pm_log_srv_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_log_srv_options

  case $state in
    pm-log-srv)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_log_srv-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_log_srv -N pm-log-srv
else
  # autoload from fpath, call function directly
  _shtab_pm_log_srv "$@"
fi

#compdef pm-man

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-man (e.g. ~/.local/share/zsh_completion/_pm-man).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_man_commands() {
  local _commands=(
    
  )
  _describe 'pm-man commands' _commands
}

_shtab_pm_man_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--format[table \= UTF-8 box-drawing (default)\; text \= plain aligned columns]:output_format:(table text)"
  {-v,--verbose}"[show extra detail (and an example, where one exists) for each command]"
  "--no-color[disable ANSI colour]"
  "--completion[print the tab-completion script for every pm-\* command and exit]:SHELL:(bash zsh)"
  ":a pm-\* command name (e.g. pm-viewer) to show its full man page:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_man_defaults_added=0


_shtab_pm_man() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-man'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_man_defaults_added )); then
    if (( ${_shtab_pm_man_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_man_options[(I)${(q)remainder}*]} +          ${_shtab_pm_man_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_man_options+=(': :_shtab_pm_man_commands' '*::: :->pm-man')
    fi
    _shtab_pm_man_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_man_options

  case $state in
    pm-man)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_man-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_man -N pm-man
else
  # autoload from fpath, call function directly
  _shtab_pm_man "$@"
fi

#compdef pm-md-gwy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-md-gwy (e.g. ~/.local/share/zsh_completion/_pm-md-gwy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_md_gwy_commands() {
  local _commands=(
    
  )
  _describe 'pm-md-gwy commands' _commands
}

_shtab_pm_md_gwy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--bind[TCP bind address override]:bind:"
  "--port[TCP bind port override]:port:"
  "--engine-pub[Engine PUB socket address (overrides config\; default\: tcp\:\/\/127.0.0.1\:5556)]:engine_pub:"
  "--index-pub[Index PUB socket address (default\: tcp\:\/\/127.0.0.1\:5558)]:index_pub:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_md_gwy_defaults_added=0


_shtab_pm_md_gwy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-md-gwy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_md_gwy_defaults_added )); then
    if (( ${_shtab_pm_md_gwy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_md_gwy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_md_gwy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_md_gwy_options+=(': :_shtab_pm_md_gwy_commands' '*::: :->pm-md-gwy')
    fi
    _shtab_pm_md_gwy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_md_gwy_options

  case $state in
    pm-md-gwy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_md_gwy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_md_gwy -N pm-md-gwy
else
  # autoload from fpath, call function directly
  _shtab_pm_md_gwy "$@"
fi

#compdef pm-mm-bot

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-mm-bot (e.g. ~/.local/share/zsh_completion/_pm-mm-bot).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_mm_bot_commands() {
  local _commands=(
    
  )
  _describe 'pm-mm-bot commands' _commands
}

_shtab_pm_mm_bot_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--config[YAML file supplying any of these flags by long name (dashes as underscores)\; an explicit CLI flag overrides the same key from the file]:PATH:"
  "--symbol[Instrument to make a market in (e.g. AAPL) \— required unless --symbols or --config supplies one or more symbols]:symbol:"
  "--symbols[Comma-separated symbols to make markets in from one process (e.g. AAPL,MSFT) \— mutually exclusive with --symbol\; each symbol runs through the same startup\/failure-isolation checks independently (see docs-design\/EduMatcher-MM-Bot-review.md \§5a)]:symbols:"
  "--label[Override the gateway-ID symbol segment (default\: the single --symbol, or SYM1_SYM2_... derived from --symbols) \— mainly useful to keep a multi-symbol gateway ID short]:label:"
  "--strategy[Pricing strategy (default\: symmetric)]:strategy:"
  "--gap[Total spread in price units (default\: 0.10)]:gap:"
  "--max-position[Net position (either direction) at which inventory skewing saturates. Required when --strategy inventory_skew\; unused by every other strategy]:max_position:"
  "--qty[Quote size on each leg (default\: 500)]:qty:"
  "--id-suffix[Running number for gateway ID (default\: 01)]:id_suffix:"
  "--drift-ticks[Reprice when mid moves by this many ticks (default\: 3)]:drift_ticks:"
  "--reissue-delay-ms[Milliseconds to wait after fill before re-issuing (default\: 200)]:reissue_delay_ms:"
  "--tif[Time-in-force for quote legs (default\: DAY)]:tif:(DAY GTC)"
  "--heartbeat-interval-sec[Periodic live-quote check interval (default\: 5.0)]:heartbeat_interval_sec:"
  "--startup-session-timeout-sec[Max wait for first session.state event (default\: 5.0)]:startup_session_timeout_sec:"
  "--bootstrap-timeout-sec[Max wait for QBOOT reply (default\: 1.0)]:bootstrap_timeout_sec:"
  "--cancel-timeout-sec[Max wait for cancel confirmation (default\: 1.0)]:cancel_timeout_sec:"
  "--shutdown-timeout-sec[Max wait for cancel on shutdown (default\: 2.0)]:shutdown_timeout_sec:"
  "--qlegs-reconcile-interval-sec[Interval for QLEGS snapshot reconciliation (default\: 15.0)]:qlegs_reconcile_interval_sec:"
  "--initial_min[Lower bound for random bootstrap reference price]:initial_min:"
  "--initial_max[Upper bound for random bootstrap reference price]:initial_max:"
  "--engine-pull[Engine PUSH\/PULL address]:engine_pull:"
  "--engine-pub[Engine PUB address]:engine_pub:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase verbosity (-v\: INFO \+ bot debug prints, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_mm_bot_defaults_added=0


_shtab_pm_mm_bot() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-mm-bot'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_mm_bot_defaults_added )); then
    if (( ${_shtab_pm_mm_bot_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_mm_bot_options[(I)${(q)remainder}*]} +          ${_shtab_pm_mm_bot_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_mm_bot_options+=(': :_shtab_pm_mm_bot_commands' '*::: :->pm-mm-bot')
    fi
    _shtab_pm_mm_bot_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_mm_bot_options

  case $state in
    pm-mm-bot)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_mm_bot-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_mm_bot -N pm-mm-bot
else
  # autoload from fpath, call function directly
  _shtab_pm_mm_bot "$@"
fi

#compdef pm-msgen

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-msgen (e.g. ~/.local/share/zsh_completion/_pm-msgen).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_msgen_commands() {
  local _commands=(
    check:'Fail if committed output differs from the spec'
    generate:'Write generated files'
    grep-literals:'List topic string literals a generated constant should replace'
    lint:'Validate the spec only'
  )
  _describe 'pm-msgen commands' _commands
}

_shtab_pm_msgen_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_msgen_defaults_added=0

_shtab_pm_msgen_check_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--spec[Spec root holding transports.yaml and messages\/ (default\: spec)]:DIR:"
  "--out-python[Python output directory (default\: src\/edumatcher\/models\/generated)]:DIR:"
  "--out-c[C output directory (default\: docs\/examples\/generated)]:DIR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_msgen_check_defaults_added=0

_shtab_pm_msgen_generate_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--spec[Spec root holding transports.yaml and messages\/ (default\: spec)]:DIR:"
  "--out-python[Python output directory (default\: src\/edumatcher\/models\/generated)]:DIR:"
  "--out-c[C output directory (default\: docs\/examples\/generated)]:DIR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_msgen_generate_defaults_added=0

_shtab_pm_msgen_grep_literals_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--spec[Spec root holding transports.yaml and messages\/ (default\: spec)]:DIR:"
  "--src[Source tree to scan (default\: src)]:DIR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_msgen_grep_literals_defaults_added=0

_shtab_pm_msgen_lint_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--spec[Spec root holding transports.yaml and messages\/ (default\: spec)]:DIR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_msgen_lint_defaults_added=0


_shtab_pm_msgen() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-msgen'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_msgen_defaults_added )); then
    if (( ${_shtab_pm_msgen_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_msgen_options[(I)${(q)remainder}*]} +          ${_shtab_pm_msgen_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_msgen_options+=(': :_shtab_pm_msgen_commands' '*::: :->pm-msgen')
    fi
    _shtab_pm_msgen_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_msgen_options

  case $state in
    pm-msgen)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_msgen-$line[1]:"
      case $line[1] in
        check) _arguments -C -s $_shtab_pm_msgen_check_options ;;
        generate) _arguments -C -s $_shtab_pm_msgen_generate_options ;;
        grep-literals) _arguments -C -s $_shtab_pm_msgen_grep_literals_options ;;
        lint) _arguments -C -s $_shtab_pm_msgen_lint_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_msgen -N pm-msgen
else
  # autoload from fpath, call function directly
  _shtab_pm_msgen "$@"
fi

#compdef pm-opctl-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-opctl-cli (e.g. ~/.local/share/zsh_completion/_pm-opctl-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_opctl_cli_commands() {
  local _commands=(
    clear:'Delete persisted data under the data directory. Exactly one of --state or --all is required; configuration under ref_data/ is never touched.'
    down:'stop processes started by pm-opctl'
    health:'check health of every process in the active profile'
    init:'create the built-in process profile configuration'
    kill:'send SIGTERM to every process whose command line contains pm-'
    list:'list status for the active process profile'
    show:'print version, data directory and configuration paths'
    start:'start a named process profile'
    stop:'stop processes started by pm-opctl'
    up:'start a named process profile'
  )
  _describe 'pm-opctl-cli commands' _commands
}

_shtab_pm_opctl_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_defaults_added=0

_shtab_pm_opctl_cli_clear_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--state[delete persisted engine\/session state (order books, GTC orders, stats, index\/clearing state) but keep logs and audit files]"
  "--all[delete all data, including logs and audit files]"
  "--yes[skip the confirmation prompt]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_clear_defaults_added=0

_shtab_pm_opctl_cli_down_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_down_defaults_added=0

_shtab_pm_opctl_cli_health_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-q,--quiet}"[print nothing\; exit 0 when all processes are running, 1 otherwise]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_health_defaults_added=0

_shtab_pm_opctl_cli_init_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_init_defaults_added=0

_shtab_pm_opctl_cli_kill_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_kill_defaults_added=0

_shtab_pm_opctl_cli_list_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-y,--restart}"[restart dead processes without asking]"
  "--no-restart[never offer to restart dead processes]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_list_defaults_added=0

_shtab_pm_opctl_cli_show_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--json[emit machine-readable JSON instead of plain text]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_show_defaults_added=0

_shtab_pm_opctl_cli_start_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-d,--debug,-vv}"[start every process with --log-level DEBUG]"
  ":config_name:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_start_defaults_added=0

_shtab_pm_opctl_cli_stop_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_stop_defaults_added=0

_shtab_pm_opctl_cli_up_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-d,--debug,-vv}"[start every process with --log-level DEBUG]"
  ":config_name:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_opctl_cli_up_defaults_added=0


_shtab_pm_opctl_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-opctl-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_opctl_cli_defaults_added )); then
    if (( ${_shtab_pm_opctl_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_opctl_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_opctl_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_opctl_cli_options+=(': :_shtab_pm_opctl_cli_commands' '*::: :->pm-opctl-cli')
    fi
    _shtab_pm_opctl_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_opctl_cli_options

  case $state in
    pm-opctl-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_opctl_cli-$line[1]:"
      case $line[1] in
        clear) _arguments -C -s $_shtab_pm_opctl_cli_clear_options ;;
        down) _arguments -C -s $_shtab_pm_opctl_cli_down_options ;;
        health) _arguments -C -s $_shtab_pm_opctl_cli_health_options ;;
        init) _arguments -C -s $_shtab_pm_opctl_cli_init_options ;;
        kill) _arguments -C -s $_shtab_pm_opctl_cli_kill_options ;;
        list) _arguments -C -s $_shtab_pm_opctl_cli_list_options ;;
        show) _arguments -C -s $_shtab_pm_opctl_cli_show_options ;;
        start) _arguments -C -s $_shtab_pm_opctl_cli_start_options ;;
        stop) _arguments -C -s $_shtab_pm_opctl_cli_stop_options ;;
        up) _arguments -C -s $_shtab_pm_opctl_cli_up_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_opctl_cli -N pm-opctl-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_opctl_cli "$@"
fi

#compdef pm-orders

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-orders (e.g. ~/.local/share/zsh_completion/_pm-orders).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_orders_commands() {
  local _commands=(
    
  )
  _describe 'pm-orders commands' _commands
}

_shtab_pm_orders_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  {--gateway,-g}"[Filter to a single gateway (default\: show all)]:GW_ID:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_orders_defaults_added=0


_shtab_pm_orders() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-orders'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_orders_defaults_added )); then
    if (( ${_shtab_pm_orders_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_orders_options[(I)${(q)remainder}*]} +          ${_shtab_pm_orders_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_orders_options+=(': :_shtab_pm_orders_commands' '*::: :->pm-orders')
    fi
    _shtab_pm_orders_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_orders_options

  case $state in
    pm-orders)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_orders-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_orders -N pm-orders
else
  # autoload from fpath, call function directly
  _shtab_pm_orders "$@"
fi

#compdef pm-ralf-gwy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-ralf-gwy (e.g. ~/.local/share/zsh_completion/_pm-ralf-gwy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_ralf_gwy_commands() {
  local _commands=(
    
  )
  _describe 'pm-ralf-gwy commands' _commands
}

_shtab_pm_ralf_gwy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--bind[TCP bind address override]:bind:"
  "--port[TCP bind port override]:port:"
  "--engine-pub[Engine PUB socket address (default\: value from config, then tcp\:\/\/127.0.0.1\:5556)]:engine_pub:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_ralf_gwy_defaults_added=0


_shtab_pm_ralf_gwy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-ralf-gwy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_ralf_gwy_defaults_added )); then
    if (( ${_shtab_pm_ralf_gwy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_ralf_gwy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_ralf_gwy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_ralf_gwy_options+=(': :_shtab_pm_ralf_gwy_commands' '*::: :->pm-ralf-gwy')
    fi
    _shtab_pm_ralf_gwy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_ralf_gwy_options

  case $state in
    pm-ralf-gwy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_ralf_gwy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_ralf_gwy -N pm-ralf-gwy
else
  # autoload from fpath, call function directly
  _shtab_pm_ralf_gwy "$@"
fi

#compdef pm-ralf-spy

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-ralf-spy (e.g. ~/.local/share/zsh_completion/_pm-ralf-spy).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_ralf_spy_commands() {
  local _commands=(
    
  )
  _describe 'pm-ralf-spy commands' _commands
}

_shtab_pm_ralf_spy_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--host[pm-ralf-gwy TCP host (default\: 127.0.0.1)]:host:"
  "--port[pm-ralf-gwy TCP port (default\: 5580)]:port:"
  "--client-name[HELLO\|CLIENT\= identifier reported in gateway logs (default\: ralf-spy-\<pid\>)]:NAME:"
  "--role[HELLO\|ROLE\= to authenticate as (default\: AUDIT). AUDIT is entitled to every channel\; CLEARING\/DROP_COPY are entitled only to their own same-named channel.]:role:(CLEARING DROP_COPY AUDIT)"
  "--channels[Comma-separated channels to subscribe to, e.g. CLEARING,DROP_COPY. \'\*\' (default) subscribes to every channel --role is entitled to (all three for AUDIT\; just its own name for CLEARING\/DROP_COPY).]:CH[,CH...]:"
  "--symbols[Comma-separated symbols to subscribe to, e.g. AAPL,MSFT. \'\*\' (default) subscribes to every symbol -- RALF has no per-channel wildcard restriction the way CALF does.]:SYM[,SYM...]:"
  "--lastseq[Request replay on connect via HELLO\|LASTSEQ\=N for every channel --role is entitled to (0 \= no replay, default). Unlike CALF\'s separate RESUME command, RALF replay is requested directly on HELLO and is not scoped to a single channel\/symbol.]:N:"
  "--ping-interval[Send PING to the gateway every SECONDS seconds to avoid its idle timeout (default\: 60). The gateway replies PONG (hidden by default, see --show-heartbeats). Set to 0 to disable.]:SECONDS:"
  "--format[Output format (default\: human)]:format:(human json)"
  "--raw[Also echo the raw wire line under each formatted line (human format only\; ignored in json format, where the parsed record already carries every field verbatim)]"
  "--no-color[Disable ANSI colour even when stdout is a terminal]"
  "--show-heartbeats[Also print HB heartbeat lines and PONG keep-alive replies (suppressed by default to reduce noise\; they still keep the connection alive either way)]"
  "--count[Exit after N data-carrying lines (0 \= run until Ctrl-C, default). Heartbeats do not count towards N.]:N:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file. Note\: stdout is also used for --format json data output, so server (default) or file is recommended if a server isn\'t running]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_ralf_spy_defaults_added=0


_shtab_pm_ralf_spy() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-ralf-spy'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_ralf_spy_defaults_added )); then
    if (( ${_shtab_pm_ralf_spy_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_ralf_spy_options[(I)${(q)remainder}*]} +          ${_shtab_pm_ralf_spy_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_ralf_spy_options+=(': :_shtab_pm_ralf_spy_commands' '*::: :->pm-ralf-spy')
    fi
    _shtab_pm_ralf_spy_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_ralf_spy_options

  case $state in
    pm-ralf-spy)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_ralf_spy-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_ralf_spy -N pm-ralf-spy
else
  # autoload from fpath, call function directly
  _shtab_pm_ralf_spy "$@"
fi

#compdef pm-scheduler

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-scheduler (e.g. ~/.local/share/zsh_completion/_pm-scheduler).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_scheduler_commands() {
  local _commands=(
    
  )
  _describe 'pm-scheduler commands' _commands
}

_shtab_pm_scheduler_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--now[Rapid-fire all transitions immediately (for testing)]"
  "--daily[Run continuously, repeating the schedule every working day (weekends and bank holidays for the configured country are skipped)]"
  "--delay[Seconds between transitions in --now mode (default\: 3.0\; ignored outside --now)]:delay:"
  "--no-confirm[Do not query\/confirm session state via the engine]"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{--verbose,-v}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_scheduler_defaults_added=0


_shtab_pm_scheduler() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-scheduler'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_scheduler_defaults_added )); then
    if (( ${_shtab_pm_scheduler_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_scheduler_options[(I)${(q)remainder}*]} +          ${_shtab_pm_scheduler_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_scheduler_options+=(': :_shtab_pm_scheduler_commands' '*::: :->pm-scheduler')
    fi
    _shtab_pm_scheduler_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_scheduler_options

  case $state in
    pm-scheduler)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_scheduler-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_scheduler -N pm-scheduler
else
  # autoload from fpath, call function directly
  _shtab_pm_scheduler "$@"
fi

#compdef pm-setup

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-setup (e.g. ~/.local/share/zsh_completion/_pm-setup).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_setup_commands() {
  local _commands=(
    
  )
  _describe 'pm-setup commands' _commands
}

_shtab_pm_setup_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--data-dir[Data directory for persistent engine files (default\: \$EDUMATCHER_DATA_DIR, or \<repo\>\/src\/data in a source checkout, or \~\/.local\/share\/edumatcher)]:PATH:"
  "--show[Print the data directory that would be used and exit]"
  "--config[Bundled example config to deploy, e.g. \'s1-basic\', \'s3-nominal\', \'s10-complex\', or \'s150-basic\' (resolves under docs\/examples\/ref_data\/\; append \'-nomm\' for the no-market-maker-quotes variant, e.g. \'s1-basic-nomm\'\; default\: \'s3-basic\')]:NAME:"
  "--force[Recompile and overwrite an already-deployed configuration]"
  "--no-config[Only create the data directory\; do not deploy an example config]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_setup_defaults_added=0


_shtab_pm_setup() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-setup'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_setup_defaults_added )); then
    if (( ${_shtab_pm_setup_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_setup_options[(I)${(q)remainder}*]} +          ${_shtab_pm_setup_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_setup_options+=(': :_shtab_pm_setup_commands' '*::: :->pm-setup')
    fi
    _shtab_pm_setup_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_setup_options

  case $state in
    pm-setup)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_setup-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_setup -N pm-setup
else
  # autoload from fpath, call function directly
  _shtab_pm_setup "$@"
fi

#compdef pm-stats

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-stats (e.g. ~/.local/share/zsh_completion/_pm-stats).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_stats_commands() {
  local _commands=(
    
  )
  _describe 'pm-stats commands' _commands
}

_shtab_pm_stats_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--db[SQLite database path (default\: \<EDUMATCHER_DATA_DIR\>\/stats.db)]:PATH:"
  "--snapshot-interval[Seconds between price_snapshots rows per symbol (default\: 900 \= 15 min). Use a smaller value for higher-resolution intraday history, e.g. 60 for one-minute snapshots.]:SEC:"
  "--timezone[Exchange session timezone that defines the trading date used by the date columns of daily_stats and index_daily_stats (IANA name, e.g. Europe\/Stockholm\; default\: UTC). Must match pm-clearing\'s --timezone or the two daily rollups will not reconcile.]:TZ:"
  "--sql-trace[Log executed SQLite statements from the stats writer connection]"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_defaults_added=0


_shtab_pm_stats() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-stats'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_stats_defaults_added )); then
    if (( ${_shtab_pm_stats_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_stats_options[(I)${(q)remainder}*]} +          ${_shtab_pm_stats_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_stats_options+=(': :_shtab_pm_stats_commands' '*::: :->pm-stats')
    fi
    _shtab_pm_stats_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_stats_options

  case $state in
    pm-stats)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_stats-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_stats -N pm-stats
else
  # autoload from fpath, call function directly
  _shtab_pm_stats "$@"
fi

#compdef pm-stats-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-stats-cli (e.g. ~/.local/share/zsh_completion/_pm-stats-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_stats_cli_commands() {
  local _commands=(
    daily:'Show daily OHLCV summary rows'
    dates:'List available trading dates'
    gaps:'Show detected feed gaps (trades the recorder never received)'
    health:'Check the pm-stats process and stats database read/write health'
    index-daily:'Show daily index OHLC summary rows'
    index-ids:'List index IDs present in stats DB'
    index-snapshots:'Show intraday index level history'
    instruments:'Show instrument reference data (tick scale per symbol)'
    order-events:'Show private order lifecycle events'
    order-lifecycle:'Show all events for one order ID'
    snapshots:'Show intraday price snapshots'
    symbols:'List symbols present in stats DB'
    trades:'Show matched trades'
  )
  _describe 'pm-stats-cli commands' _commands
}

_shtab_pm_stats_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--db[Statistics SQLite DB path (default\: \<EDUMATCHER_DATA_DIR\>\/stats.db)]:PATH:"
  "--format[Output format (table, json, csv)]:format:(table json csv)"
  "--no-header[Suppress header row for table and csv output]"
  "--timezone[Override the session timezone that --date resolves in. By default the timezone the database was recorded with is used, so this is rarely needed\; overriding with a different value warns, because --date will then select a different trading day than pm-stats used]:TZ:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_defaults_added=0

_shtab_pm_stats_cli_daily_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from-date[Start of an inclusive multi-day range (oldest first)]:YYYY-MM-DD:"
  "--to-date[End of an inclusive multi-day range]:YYYY-MM-DD:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--limit[N]:N:"
  "--after[Opaque cursor from a previous run\'s truncation notice\; fetches the next page]:CURSOR:"
  "--wide[Include bid\/ask and largest-trade columns]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_daily_defaults_added=0

_shtab_pm_stats_cli_dates_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--symbol[SYMBOL]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_dates_defaults_added=0

_shtab_pm_stats_cli_gaps_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[ISO_TS]:ISO_TS:"
  "--to[ISO_TS]:ISO_TS:"
  "--limit[N]:N:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_gaps_defaults_added=0

_shtab_pm_stats_cli_health_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-q,--quiet}"[Print nothing\; exit 0 when healthy or -1 otherwise]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_health_defaults_added=0

_shtab_pm_stats_cli_index_daily_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from-date[Start of an inclusive multi-day range (oldest first)]:YYYY-MM-DD:"
  "--to-date[End of an inclusive multi-day range]:YYYY-MM-DD:"
  "--index-id[INDEX_ID]:INDEX_ID:"
  "--limit[N]:N:"
  "--after[Opaque cursor from a previous run\'s truncation notice\; fetches the next page]:CURSOR:"
  "--wide[Include open\/close aggregate market cap columns]"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_index_daily_defaults_added=0

_shtab_pm_stats_cli_index_ids_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_index_ids_defaults_added=0

_shtab_pm_stats_cli_index_snapshots_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--index-id[INDEX_ID]:INDEX_ID:"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[ISO_TS]:ISO_TS:"
  "--to[ISO_TS]:ISO_TS:"
  "--limit[N]:N:"
  "--after[Opaque cursor from a previous run\'s truncation notice\; fetches the next page]:CURSOR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_index_snapshots_defaults_added=0

_shtab_pm_stats_cli_instruments_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--symbol[SYMBOL]:SYMBOL:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_instruments_defaults_added=0

_shtab_pm_stats_cli_order_events_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GATEWAY_ID]:GATEWAY_ID:"
  "--symbol[SYMBOL]:SYMBOL:"
  "--event-type[One of\: ACK, REJECT, FILL, AMEND, CANCEL, EXPIRE, COMBO_ACK, COMBO_REJECT, COMBO_STATUS, OCO_ACK, OCO_REJECT, OCO_CANCEL, QUOTE_ACK, QUOTE_REJECT, QUOTE_STATUS, UNKNOWN, EVENT]:TYPE:(ACK REJECT FILL AMEND CANCEL EXPIRE COMBO_ACK COMBO_REJECT COMBO_STATUS OCO_ACK OCO_REJECT OCO_CANCEL QUOTE_ACK QUOTE_REJECT QUOTE_STATUS UNKNOWN EVENT)"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[ISO_TS]:ISO_TS:"
  "--to[ISO_TS]:ISO_TS:"
  "--limit[N]:N:"
  "--after[Opaque cursor from a previous run\'s truncation notice\; fetches the next page]:CURSOR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_order_events_defaults_added=0

_shtab_pm_stats_cli_order_lifecycle_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--gateway[GATEWAY_ID]:GATEWAY_ID:"
  "--order-id[ORDER_ID]:ORDER_ID:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_order_lifecycle_defaults_added=0

_shtab_pm_stats_cli_snapshots_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--symbol[SYMBOL]:SYMBOL:"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[ISO_TS]:ISO_TS:"
  "--to[ISO_TS]:ISO_TS:"
  "--limit[N]:N:"
  "--after[Opaque cursor from a previous run\'s truncation notice\; fetches the next page]:CURSOR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_snapshots_defaults_added=0

_shtab_pm_stats_cli_symbols_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_symbols_defaults_added=0

_shtab_pm_stats_cli_trades_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--symbol[SYMBOL]:SYMBOL:"
  "--date[YYYY-MM-DD]:YYYY-MM-DD:"
  "--from[ISO_TS]:ISO_TS:"
  "--to[ISO_TS]:ISO_TS:"
  "--limit[N]:N:"
  "--after[Opaque cursor from a previous run\'s truncation notice\; fetches the next page]:CURSOR:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_stats_cli_trades_defaults_added=0


_shtab_pm_stats_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-stats-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_stats_cli_defaults_added )); then
    if (( ${_shtab_pm_stats_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_stats_cli_options[(I)${(q)remainder}*]} +          ${_shtab_pm_stats_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_stats_cli_options+=(': :_shtab_pm_stats_cli_commands' '*::: :->pm-stats-cli')
    fi
    _shtab_pm_stats_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_stats_cli_options

  case $state in
    pm-stats-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_stats_cli-$line[1]:"
      case $line[1] in
        daily) _arguments -C -s $_shtab_pm_stats_cli_daily_options ;;
        dates) _arguments -C -s $_shtab_pm_stats_cli_dates_options ;;
        gaps) _arguments -C -s $_shtab_pm_stats_cli_gaps_options ;;
        health) _arguments -C -s $_shtab_pm_stats_cli_health_options ;;
        index-daily) _arguments -C -s $_shtab_pm_stats_cli_index_daily_options ;;
        index-ids) _arguments -C -s $_shtab_pm_stats_cli_index_ids_options ;;
        index-snapshots) _arguments -C -s $_shtab_pm_stats_cli_index_snapshots_options ;;
        instruments) _arguments -C -s $_shtab_pm_stats_cli_instruments_options ;;
        order-events) _arguments -C -s $_shtab_pm_stats_cli_order_events_options ;;
        order-lifecycle) _arguments -C -s $_shtab_pm_stats_cli_order_lifecycle_options ;;
        snapshots) _arguments -C -s $_shtab_pm_stats_cli_snapshots_options ;;
        symbols) _arguments -C -s $_shtab_pm_stats_cli_symbols_options ;;
        trades) _arguments -C -s $_shtab_pm_stats_cli_trades_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_stats_cli -N pm-stats-cli
else
  # autoload from fpath, call function directly
  _shtab_pm_stats_cli "$@"
fi

#compdef pm-ticker

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-ticker (e.g. ~/.local/share/zsh_completion/_pm-ticker).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_ticker_commands() {
  local _commands=(
    
  )
  _describe 'pm-ticker commands' _commands
}

_shtab_pm_ticker_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  "--db[Statistics SQLite database (default\: \<EDUMATCHER_DATA_DIR\>\/stats.db)]:PATH:"
  "--db-interval[Seconds between daily_stats DB re-queries (default\: 900)]:SEC:"
  "--timezone[Override the session timezone used to resolve today\'s trading date. By default the timezone the statistics database was recorded with is used, which is almost always what you want]:TZ:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_ticker_defaults_added=0


_shtab_pm_ticker() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-ticker'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_ticker_defaults_added )); then
    if (( ${_shtab_pm_ticker_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_ticker_options[(I)${(q)remainder}*]} +          ${_shtab_pm_ticker_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_ticker_options+=(': :_shtab_pm_ticker_commands' '*::: :->pm-ticker')
    fi
    _shtab_pm_ticker_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_ticker_options

  case $state in
    pm-ticker)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_ticker-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_ticker -N pm-ticker
else
  # autoload from fpath, call function directly
  _shtab_pm_ticker "$@"
fi

#compdef pm-viewer

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _pm-viewer (e.g. ~/.local/share/zsh_completion/_pm-viewer).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_pm_viewer_commands() {
  local _commands=(
    
  )
  _describe 'pm-viewer commands' _commands
}

_shtab_pm_viewer_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)--version[show program\'s version number and exit]"
  {--symbol,-s}"[Symbol to watch, e.g. AAPL]:SYMBOL:"
  {--depth,-d}"[Max price levels to display (default\: fit to terminal height)]:depth:"
  "--db[Statistics SQLite DB for seed OHLC (default\: \<EDUMATCHER_DATA_DIR\>\/stats.db)]:PATH:"
  "--text-color[Body text color for the order book tables \— a \#rrggbb hex code or a Rich color name (default\: white)]:COLOR:"
  "--zebra-lines[Shade every second row of the BIDS\/ASKS\/TRADES tables with a background tint for readability (default\: off)]"
  "--zebra-lines-color[Background tint used by --zebra-lines \— a \#rrggbb hex code or a Rich color name (default\: grey19, a dark grey). Has no effect unless --zebra-lines is given.]:COLOR:"
  "--log-level[Logging level override (default\: WARNING)]:log_level:(CRITICAL ERROR WARNING INFO DEBUG)"
  "*"{-v,--verbose}"[Increase log verbosity (-v\: INFO, -vv\: DEBUG)]"
  {-q,--quiet}"[Reduce log output to warnings\/errors]"
  "--log-target[Where this process\'s own operational log records go\: server (default, auto-detected pm-log-srv), stdout, or file]:log_target:(server stdout file)"
  "--log-file[Operational log file path \— required when --log-target file]:PATH:"
  "--log-failover-timeout[Grace window before falling back to a local log file once pm-log-srv becomes unreachable (default\: 30, from config)]:SECONDS:"
)

# guard to ensure default positional specs are added only once per session
_shtab_pm_viewer_defaults_added=0


_shtab_pm_viewer() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->pm-viewer'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_pm_viewer_defaults_added )); then
    if (( ${_shtab_pm_viewer_options[(I)${(q)one_or_more}*]} +          ${_shtab_pm_viewer_options[(I)${(q)remainder}*]} +          ${_shtab_pm_viewer_options[(I)${(q)default}]} == 0 )); then
      _shtab_pm_viewer_options+=(': :_shtab_pm_viewer_commands' '*::: :->pm-viewer')
    fi
    _shtab_pm_viewer_defaults_added=1
  fi
  _arguments -C -s $_shtab_pm_viewer_options

  case $state in
    pm-viewer)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_pm_viewer-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_pm_viewer -N pm-viewer
else
  # autoload from fpath, call function directly
  _shtab_pm_viewer "$@"
fi
