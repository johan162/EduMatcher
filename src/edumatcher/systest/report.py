"""Human-readable, JSON, and JUnit XML reporters.

Renders one ``AssertionReport`` + ``CoverageLedger`` pair into a
human-readable text summary, a JSON artefact, and a JUnit XML report, with
each Scenario x Binding pair represented as one JUnit test case.

See design.md Components §17 (Reporter).
"""
