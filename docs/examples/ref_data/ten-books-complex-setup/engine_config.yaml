# Generated by pm-config-gen v1.1.0 on 2026-06-21
# Command: pm-config-gen --symbols AAPL MSFT TSLA AMZN GOOGL META NVDA NFLX INTC ORCL --gateways TRADER01:TRADER:CANCEL_ALL TRADER02:TRADER:CANCEL_ALL TRADER03:TRADER:CANCEL_ALL TRADER04:TRADER:CANCEL_ALL TRADER05:TRADER:CANCEL_ALL OPS01:ADMIN:LEAVE_ALL MM01:MARKET_MAKER:CANCEL_QUOTES_ONLY MM02:MARKET_MAKER:CANCEL_QUOTES_ONLY --output engine_config.yaml --force --sessions-enabled --schedule --pre-open 08:45 --opening-auction 08:55 --continuous 09:00 --closing-auction 16:00 --closing-end 16:10 --snapshot-interval 0.25 --static-band 0.20 --dynamic-band 0.02 --risk-level CORE:0.18:0.02 --risk-level HIGH_BETA:0.12:0.04 --cb-levels L1:0.07:5 L2:0.13:15 L3:0.20:0 --cb-window-ns 300000000000 --mm-spread-ticks 12 --mm-min-qty 200 --enforce-mm-obligations --tick-decimals 2 --seed-last-prices --symbol-opts AAPL:level=CORE,mm_spread_ticks=8,mm_min_qty=300 --symbol-opts TSLA:level=HIGH_BETA,dynamic_band=0.04,cb_halt_l1=10 --post-trade-gateway --post-trade-name ralf-gwy01 --post-trade-bind-address 127.0.0.1 --post-trade-port 5580 --post-trade-replay-retention-sec 14400 --post-trade-heartbeat-interval-sec 1 --post-trade-idle-timeout-sec 10 --post-trade-max-client-queue 8000 --post-trade-allowed-roles CLEARING DROP_COPY AUDIT --market-data-gateway --market-data-enabled --market-data-name md-gwy01 --market-data-bind-address 127.0.0.1 --market-data-port 5570 --market-data-heartbeat-interval-sec 1 --market-data-idle-timeout-sec 5 --market-data-replay-window-sec 120 --market-data-max-symbols-per-client 500 --market-data-max-client-queue 20000
#
# Validate with:
#   poetry run python -c 'from pathlib import Path; \
#     from edumatcher.engine.config_loader import load_engine_config; \
#     print(load_engine_config(Path("engine_config.yaml")))'

# -- Session control --
sessions_enabled: true

# -- Engine behavior --
enforce_collars: true
enforce_circuit_breakers: true
snapshot_interval_sec: 0.25

# -- Market-maker obligation defaults --
mm_obligation_defaults:
  enforce_mm_obligation: true
  mm_max_spread_ticks: 12
  mm_min_qty: 200
  symbols:
    AAPL:
      enforce_mm_obligation: true
      mm_max_spread_ticks: 8
      mm_min_qty: 300

# -- Collar profiles --
risk_controls:
  levels:
    DEFAULT:
      collar:
        static_band_pct: 0.2
        dynamic_band_pct: 0.02
    CORE:
      collar:
        static_band_pct: 0.18
        dynamic_band_pct: 0.02
    HIGH_BETA:
      collar:
        static_band_pct: 0.12
        dynamic_band_pct: 0.04
  default_level: DEFAULT

# -- Circuit-breaker ladder --
circuit_breaker_defaults:
  reference_window_ns: 300000000000
  levels:
    L1:
      price_shift_pct: 0.07
      halt_duration_ns: 300000000000
      resumption_mode: AUCTION
    L2:
      price_shift_pct: 0.13
      halt_duration_ns: 900000000000
      resumption_mode: AUCTION
    L3:
      price_shift_pct: 0.2
      halt_duration_ns: null    # null = rest-of-day halt
      resumption_mode: AUCTION

# -- ALF gateway allowlist --
gateways:
  alf:
  - id: TRADER01
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: TRADER02
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: TRADER03
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: TRADER04
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: TRADER05
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: OPS01
    role: ADMIN
    disconnect_behaviour: LEAVE_ALL
  - id: MM01
    role: MARKET_MAKER
    disconnect_behaviour: CANCEL_QUOTES_ONLY
    quote_refresh_policy: INACTIVATE_ON_ANY_FILL
  - id: MM02
    role: MARKET_MAKER
    disconnect_behaviour: CANCEL_QUOTES_ONLY
    quote_refresh_policy: INACTIVATE_ON_ANY_FILL

# -- RALF post-trade gateway --
post_trade_gateway:
  name: ralf-gwy01
  bind_address: 127.0.0.1
  port: 5580
  replay_retention_sec: 14400
  heartbeat_interval_sec: 1
  idle_timeout_sec: 10
  max_client_queue: 8000
  allowed_roles:
  - CLEARING
  - DROP_COPY
  - AUDIT

# -- Symbol universe --
symbols:
  AAPL:
    tick_decimals: 2
    level: CORE
    last_buy_price: 283.50    # REQUIRED: set last buy reference price
    last_sell_price: 284.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 283.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 284.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 283.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 284.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  MSFT:
    tick_decimals: 2
    last_buy_price: 63.50    # REQUIRED: set last buy reference price
    last_sell_price: 64.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 63.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 64.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 63.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 64.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  TSLA:
    tick_decimals: 2
    level: HIGH_BETA
    last_buy_price: 182.50    # REQUIRED: set last buy reference price
    last_sell_price: 183.50    # REQUIRED: set last sell reference price
    collar:
      dynamic_band_pct: 0.04
    circuit_breaker:
      levels:
        L1:
          halt_duration_ns: 600000000000
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 182.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 183.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 182.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 183.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  AMZN:
    tick_decimals: 2
    last_buy_price: 281.50    # REQUIRED: set last buy reference price
    last_sell_price: 282.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 281.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 282.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 281.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 282.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  GOOGL:
    tick_decimals: 2
    last_buy_price: 45.50    # REQUIRED: set last buy reference price
    last_sell_price: 46.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 45.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 46.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 45.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 46.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  META:
    tick_decimals: 2
    last_buy_price: 253.50    # REQUIRED: set last buy reference price
    last_sell_price: 254.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 253.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 254.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 253.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 254.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  NVDA:
    tick_decimals: 2
    last_buy_price: 37.50    # REQUIRED: set last buy reference price
    last_sell_price: 38.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 37.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 38.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 37.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 38.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  NFLX:
    tick_decimals: 2
    last_buy_price: 227.50    # REQUIRED: set last buy reference price
    last_sell_price: 228.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 227.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 228.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 227.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 228.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  INTC:
    tick_decimals: 2
    last_buy_price: 94.50    # REQUIRED: set last buy reference price
    last_sell_price: 95.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 94.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 95.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 94.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 95.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  ORCL:
    tick_decimals: 2
    last_buy_price: 67.50    # REQUIRED: set last buy reference price
    last_sell_price: 68.50    # REQUIRED: set last sell reference price
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 67.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 68.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    - gateway_id: MM02
      bid_price: 67.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 68.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true

# -- Session schedule --
schedule:
  pre_open: 08:45
  opening_auction_start: 08:55
  continuous_start: 09:00
  closing_auction_start: '16:00'
  closing_auction_end: '16:10'

# -- Additional fields --
market_data_gateway:
  enabled: true
  name: md-gwy01
  bind_address: 127.0.0.1
  port: 5570
  heartbeat_interval_sec: 1
  idle_timeout_sec: 5
  replay_window_sec: 120
  max_symbols_per_client: 500
  max_client_queue: 20000

market_maker_combos:
  - combo_id: SEED-PAIR-AAPL-MSFT
    combo_type: AON
    tif: DAY
    legs:
      - symbol: AAPL
        side: BUY
        order_type: LIMIT
        quantity: 100
        price: 28400
        stop_price:
        smp_action: NONE
      - symbol: MSFT
        side: SELL
        order_type: LIMIT
        quantity: 100
        price: 6400
        stop_price:
        smp_action: NONE
