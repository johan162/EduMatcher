# Generated by pm-config-gen v1.1.0 on 2026-06-21
# Command: pm-config-gen --symbols AAPL MSFT TSLA AMZN GOOGL META NVDA NFLX INTC ORCL IBM ADBE CRM QCOM AMD AVGO TXN NOW SHOP UBER PYPL SQ BABA SONY SAP ASML CSCO MU BKNG TSM --gateways TRADER01:TRADER:CANCEL_ALL TRADER02:TRADER:CANCEL_ALL OPS01:ADMIN:LEAVE_ALL MM01:MARKET_MAKER:CANCEL_QUOTES_ONLY --output engine_config.yaml --force --sessions-enabled --post-trade-gateway --post-trade-bind-address 127.0.0.1 --post-trade-port 5580 --post-trade-replay-retention-sec 3600 --post-trade-heartbeat-interval-sec 1 --post-trade-idle-timeout-sec 10 --post-trade-max-client-queue 5000 --post-trade-allowed-roles CLEARING DROP_COPY AUDIT --market-data-gateway --market-data-enabled --market-data-name md-gwy01 --market-data-bind-address 127.0.0.1 --market-data-port 5570 --market-data-heartbeat-interval-sec 1 --market-data-idle-timeout-sec 5 --market-data-replay-window-sec 30 --market-data-max-symbols-per-client 200 --market-data-max-client-queue 10000
#
# Validate with:
#   poetry run python -c 'from pathlib import Path; \
#     from edumatcher.engine.config_loader import load_engine_config; \
#     print(load_engine_config(Path("engine_config.yaml")))'

# -- Session control --
sessions_enabled: true

# -- Engine behavior --
enforce_collars: true
enforce_circuit_breakers: true
snapshot_interval_sec: 0.5

# -- Market-maker obligation defaults --
mm_obligation_defaults:
  enforce_mm_obligation: false
  mm_max_spread_ticks: 20
  mm_min_qty: 100

# -- ALF gateway allowlist --
gateways:
  alf:
  - id: TRADER01
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: TRADER02
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: OPS01
    role: ADMIN
    disconnect_behaviour: LEAVE_ALL
  - id: MM01
    role: MARKET_MAKER
    disconnect_behaviour: CANCEL_QUOTES_ONLY
    quote_refresh_policy: INACTIVATE_ON_ANY_FILL

# -- RALF post-trade gateway --
post_trade_gateway:
  name: ralf-gwy01
  bind_address: 127.0.0.1
  port: 5580
  replay_retention_sec: 3600
  heartbeat_interval_sec: 1
  idle_timeout_sec: 10
  max_client_queue: 5000
  allowed_roles:
  - CLEARING
  - DROP_COPY
  - AUDIT

# -- Symbol universe --
symbols:
  AAPL:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 234.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 235.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  MSFT:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 124.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 125.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  TSLA:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 153.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 154.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  AMZN:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 197.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 198.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  GOOGL:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 294.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 295.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  META:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 120.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 121.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  NVDA:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 102.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 103.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  NFLX:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 185.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 186.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  INTC:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 263.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 264.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  ORCL:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 53.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 54.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  IBM:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 114.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 115.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  ADBE:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 112.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 113.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  CRM:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 101.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 102.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  QCOM:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 71.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 72.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  AMD:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 261.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 262.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  AVGO:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 26.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 27.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  TXN:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 99.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 100.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  NOW:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 122.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 123.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  SHOP:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 287.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 288.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  UBER:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 169.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 170.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  PYPL:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 165.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 166.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  SQ:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 45.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 46.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  BABA:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 103.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 104.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  SONY:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 25.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 26.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  SAP:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 152.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 153.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  ASML:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 35.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 36.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  CSCO:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 89.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 90.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  MU:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 115.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 116.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  BKNG:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 108.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 109.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
  TSM:
    tick_decimals: 2
    market_maker_quotes:
      # WARNING: pm-config-gen cannot set prices. Fill these in before starting.
    - gateway_id: MM01
      bid_price: 255.50    # REQUIRED: set display bid price (e.g. 209.00)
      ask_price: 256.50    # REQUIRED: set display ask price (e.g. 211.00)
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true

# -- Session schedule --
schedule:
  pre_open: 09:00
  opening_auction_start: 09:25
  continuous_start: 09:30
  closing_auction_start: '16:00'
  closing_auction_end: '16:05'

# -- Additional fields --
market_data_gateway:
  enabled: true
  name: md-gwy01
  bind_address: 127.0.0.1
  port: 5570
  heartbeat_interval_sec: 1
  idle_timeout_sec: 5
  replay_window_sec: 30
  max_symbols_per_client: 200
  max_client_queue: 10000
