# Generated by pm-config-gen v1.1.0 on 2026-06-21
# Command: pm-config-gen --symbols AAPL MSFT TSLA --gateways TRADER01 TRADER02 OPS01:ADMIN MM01:MARKET_MAKER:CANCEL_QUOTES_ONLY --seed-mm-mid-range 20:300 --seed-last-prices-from-mm --output engine_config.yaml --force --comment-default-config-fields --seed 20260621 --outstanding-shares AAPL:15400000000 --outstanding-shares MSFT:7430000000 --outstanding-shares TSLA:3200000000
#
# Validate with:
#   poetry run python -c 'from pathlib import Path; \
#     from edumatcher.engine.config_loader import load_engine_config; \
#     print(load_engine_config(Path("engine_config.yaml")))'

# Defaultable engine_config fields currently using runtime defaults:
#   gateways.alf[].description = ''
#   schedule.pre_open = '09:00'
#   schedule.opening_auction_start = '09:25'
#   schedule.continuous_start = '09:30'
#   schedule.closing_auction_start = '16:00'
#   schedule.closing_auction_end = '16:05'
#   post_trade_gateway.name = 'ralf-gwy01'
#   post_trade_gateway.bind_address = '0.0.0.0'
#   post_trade_gateway.port = 5580
#   post_trade_gateway.replay_retention_sec = 86400
#   post_trade_gateway.heartbeat_interval_sec = 1
#   post_trade_gateway.idle_timeout_sec = 5
#   post_trade_gateway.max_client_queue = 10000
#   post_trade_gateway.allowed_roles = [CLEARING, DROP_COPY, AUDIT]
#   market_data_gateway.enabled = true
#   market_data_gateway.name = 'md-gwy01'
#   market_data_gateway.bind_address = '0.0.0.0'
#   market_data_gateway.port = 5570
#   market_data_gateway.heartbeat_interval_sec = 1
#   market_data_gateway.idle_timeout_sec = 5
#   market_data_gateway.replay_window_sec = 30
#   market_data_gateway.max_symbols_per_client = 200
#   market_data_gateway.max_client_queue = 10000

# -- Session control --
sessions_enabled: false

# -- Engine behavior --
enforce_collars: true
enforce_circuit_breakers: true
snapshot_interval_sec: 0.5

# -- Market-maker obligation defaults --
mm_obligation_defaults:
  enforce_mm_obligation: false
  mm_max_spread_ticks: 20
  mm_min_qty: 100

# -- ALF gateway allowlist --
gateways:
  alf:
  - id: TRADER01
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: TRADER02
    role: TRADER
    disconnect_behaviour: CANCEL_ALL
  - id: OPS01
    role: ADMIN
    disconnect_behaviour: LEAVE_ALL
  - id: MM01
    role: MARKET_MAKER
    disconnect_behaviour: CANCEL_QUOTES_ONLY
    quote_refresh_policy: INACTIVATE_ON_ANY_FILL

# -- Symbol universe --
symbols:
  AAPL:
    tick_decimals: 2
    last_buy_price: 191.86
    last_sell_price: 191.86
    market_maker_quotes:
    - gateway_id: MM01
      bid_price: 191.85
      ask_price: 191.87
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    outstanding_shares: 15400000000
  MSFT:
    tick_decimals: 2
    last_buy_price: 204.1
    last_sell_price: 204.1
    market_maker_quotes:
    - gateway_id: MM01
      bid_price: 204.09
      ask_price: 204.11
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    outstanding_shares: 7430000000
  TSLA:
    tick_decimals: 2
    last_buy_price: 141.36
    last_sell_price: 141.36
    market_maker_quotes:
    - gateway_id: MM01
      bid_price: 141.35
      ask_price: 141.37
      bid_qty: 1000
      ask_qty: 1000
      tif: DAY
      seed_once: true
    outstanding_shares: 3200000000
