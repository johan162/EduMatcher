# Deployment

Two ways to run the EduMatcher backend, pick one:

- [`docker/`](docker/) — the whole backend in one Podman/Docker container. See [docker/README.md](docker/README.md).
- [`vm/`](vm/) — a Multipass VM with the runtime installed. See [vm/README.md](vm/README.md).
